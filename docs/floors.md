# twicetold-npc — Verified floors

Split out of `status.md` on 2026-07-28 (full-repo audit). The table lived inside the
auto-loaded living file, where it cost ~8.7k tokens of every session's context and had
drifted: blank lines between rows broke the GFM table after row 8, so the ten most recent
floors did not render as a table anywhere, and the headline count disagreed with the rows.
Rows are moved **verbatim** — no cell text was edited. *(One exception, noted 2026-07-29: the
Target-A row's `Research Papers\` pointer became `docs\research\` when the audit's phase 7 moved
that folder into the repo — a path fix during propagation, not an evidence edit.)*

**Append-only.** A row lands here only after an independent floor-verifier pass returns
**pass** (CLAUDE.md: staged verification). Never edit a landed row except to add a dated
supersede note; floors are re-openable, and a re-opened floor is re-verified, not rewritten.

**Counting convention** *(stated 2026-07-28 — its absence was the source of the drift)*: the
table has **33 rows** *(18 at the 2026-07-28 split; the stage-2 Unity Play-mode gate and
eval-harness stage 1 landed 2026-07-29; the A1 re-shape landed 2026-08-04; eval-harness
stage 2 landed 2026-08-05; eval-harness stage 3 landed 2026-08-07; eval-harness stage 4
landed 2026-08-12; deferred writes (C1) built 2026-08-12, verified 2026-08-13; reflection
(C2) built + verified 2026-08-15; the parameter compiler (C3) built + verified
2026-08-17; the dissonance path (C4) built + verified 2026-08-17; the agent-state read +
async observes (C5) built + verified 2026-08-17; the purge endpoint (C6) built + verified
2026-08-18; the concurrency cap (C7 Stage A) built + verified 2026-08-18; the scene-boundary
pre-warm (C7 Stage B) built + verified 2026-08-18; the provider path + the per-agent purge
verb built + verified 2026-09-02)*, and every
row is a verified floor. Earlier prose in `status.md`
counted "sixteen" against eighteen rows because it silently excluded the two rows that are
not application layers — the Postgres/pgvector container + read-only MCP row, and the
structural pytest suite row. Both ARE verified floors and both are counted here. When you
cite a count, cite this table.

Each "Verified against" cell records what was actually re-run: the walker and its assertion
count, the prior floors re-checked, the migration state, the product-DB pristine check, and
any live beat. That cell is the evidence, not a summary of it.

---

## The table


| Layer | Verified against | Date |
|---|---|---|
| Postgres 16 + pgvector 0.8.5 container (`longmem-pg`) + read-only Postgres MCP | live: `docker` health `healthy`, `pg_available_extensions` → `vector 0.8.5`, `claude mcp list` → `postgres ✓ Connected` | 2026-07-13 |
| Migration 01 foundational schema — 9 tables, 7 CHECKs, HNSW cosine + GIN + one-live-head + FK indexes (`db\migrations\001_foundation.sql`, applied by `db\migrate.py`) | floor-verifier **pass** on live `longmem`: every done-when re-run (idempotent second run, CHECK rejection, server UUID defaults, smoke fixture) + `vector 0.8.5` enabled; DB left pristine (only `schema_migrations`) | 2026-07-13 |
| Write path v1 — ingest service seam (`app\ingest.py`) + thin FastAPI route (`app\api.py`, served via `python -m app.serve`) + real/deterministic-fake providers (`app\providers.py`) + NLP pass (`app\nlp.py`: spaCy lg + fastcoref + VADER + Warriner VAD) + atomic insert (`app\db.py`) | floor-verifier **pass** against the migration-01 floor: structural walker `tests\verify_write_path.py` re-run independently (35 assertions covering all 14 done-when criteria) on scratch `longmem_test`; `db\migrate.py` no-arg still a clean no-op on `longmem`; product `longmem` confirmed pristine via postgres MCP; independent spot-check of live head, span offsets, and degradation rows | 2026-07-13 |
| Read path v1 — retrieval service seam (`app\retrieval.py`) + shared decay math (`app\decay.py`) + thin `POST /v1/dialogue/init` route (`app\api.py`) + read-only candidate SQL (`app\db.py`) + wire models (`app\schemas.py`) + five knobs (`app\config.py`) | floor-verifier **pass** against the migration-01 + write-path floors: structural walker `tests\verify_read_path.py` re-run independently (34 assertions, done-when 1–11) on scratch `longmem_test`; `tests\verify_write_path.py` re-run (35 assertions — shared files touched, floor intact); `db\migrate.py` no-arg still a clean no-op on `longmem`; `longmem` confirmed pristine; independent read-only SQL spot-checks (live head, NULL-embedding row, one-live-head index); plus a live `python -m app.serve` route session with byte-identical repeated reads | 2026-07-14 |
| CLI harness v1 — dialogue-turn seam (`app\dialogue.py`: retrieval → prompt assembly → single dialogue call → directive validation → atomic in-place reputation apply) + shared session-runner core (`app\session.py`) + REPL (`python -m app.cli`, `app\cli.py`) + synthetic load driver (`python -m app.load_driver`, `app\load_driver.py`) + dialogue provider triad (`app\providers.py`) + turn wire models (`app\schemas.py`) + reputation SQL (`app\db.py`) + dialogue role/reputation/pricing config (`app\config.py`) | floor-verifier **pass** against the migration-01 + write-path + read-path floors: structural walker `tests\verify_cli_harness.py` re-run independently (36 assertions, every done-when criterion) on scratch `longmem_test`; both prior walkers re-run clean (35/35, 34/34 — shared files touched, floors intact); `db\migrate.py` no-arg still a clean no-op on `longmem` (schema frozen, `001` the only migration); `longmem` confirmed pristine **via the postgres MCP — the verifier's `mcp__postgres__*` tools worked this dispatch, resolving the 2026-07-14 flag**; an independent standalone load-driver run (offline, keyless); plus a live piped REPL session (observe → dialogue turns with debug view → scene-boundary snapshot refresh) *(Superseded in part by the A1 re-shape 2026-08-04 — the directive validation, the reputation apply/SQL/config, and the snapshot refresh this row lands were removed by ruling; the walker was rewritten, 51 assertions, and re-verified against the re-shaped seam.)* | 2026-07-15 |
| Authorial-correction v1 — memory-scoped operator verb (`POST /v1/memories/{id}/correction` in `app\api.py` → `IngestService.correct` in `app\ingest.py` → one-transaction `apply_authorial_correction` in `app\db.py`: predicate supersede + optional `expected_detail_id` compare-and-swap + corrected `authorial_correction` head at t_c + cache eviction with count) + the constraint-follows-anchor reconstruction delta (`app\reconstruction.py` `build_reconstruction_item`, anchor-cause-aware; `db.ReconstructionSource.anchor_cause`) + wire models (`app\schemas.py`) + REPL surface (`app\session.py` `runner.correct`, `app\cli.py` `:correct`) | floor-verifier **pass** against all five prior floors (reconstruction deliberately re-opened by the constraint-follows-anchor ruling, re-verified): structural walker `tests\verify_authorial_correction.py` re-run independently (31 assertions, every done-when criterion incl. the re-ruled stored-coherence time-travel criterion) on fresh scratch `longmem_test`; all four prior walkers re-run clean (42/42 — grown +1 by the corrected-item assertion, addition only; 36/36; 34/34; 35/35); `db\migrate.py` no-arg a clean no-op on `longmem` (no migration needed, as specced); `longmem` confirmed pristine via the postgres MCP; independent code spot-checks (transaction + CAS rollback, eviction inside the transaction, byte-verbatim no-model path, retrieval/scoring untouched vs HEAD); plus a live piped REPL correction-override beat (read verbatim → `:correct` head swap → corrected read, one scene) | 2026-07-18 |
| Reconstruction v1 — serving-stage engine (`app\reconstruction.py`: theta partition at the scene-frozen basis → decay-band cache → batched retelling call → drift-budgeted atomic write-back → serve-only-persisted-text) + serving swap (`app\retrieval.py`, retrieval/scoring untouched) + identity-document plumbing (`app\identity.py` + scene-boundary recompile in `app\ingest.py` + caller-frozen scene state in `app\session.py`) + reconstruction provider triad and the **locality-sensitive fake embedding** (`app\providers.py`) + reconstruction SQL (`app\db.py`) + wire/instrumentation deltas (`app\schemas.py`) + knobs/role/pricing (`app\config.py`) + debug/aggregate surfacing (`app\cli.py`, `app\load_driver.py`) + 422 mapping (`app\api.py`) | floor-verifier **pass** against all four prior floors: structural walker `tests\verify_reconstruction.py` re-run independently (41 assertions, every done-when criterion) on fresh scratch `longmem_test`; all three prior walkers re-run clean (35/35, 34/34, 36/36 — the read-side pair pin `reconstruction_theta = 0` in fixture configs, assertion bodies untouched, verified against git); schema frozen (`001` the only migration; migrate **`--database-uri` on `/longmem`** → "Up to date, 0 pending" — no-arg blocked by the `.env` sandbox pointer, flagged); `longmem` confirmed pristine **via the postgres MCP (tools worked this dispatch)**; independent code spot-checks (atomic write-back, derivable anchor, scene-basis binding, retrieval byte-identical to HEAD); plus a live piped REPL drift beat (verbatim → 46-day jump → reconstructed write-back → call-free cache hit) and a standalone load-driver run with the reconstruction latency/cost rows | 2026-07-17 |
| Fact-level correction v1 — **migration 002** (`db\migrations\002_fact_versions.sql`: `memory_fact_versions` fact chain + guarded backfill + one-live-head partial unique + **partial HNSW** over live heads + `memories_embedding_hnsw` **dropped**, ruled) + the fact-following verb (`app\db.py` `apply_authorial_correction` grown: fact supersede + insert in the same transaction; `app\ingest.py` embed-before-transaction + `CorrectionEmbedFailedError`; `app\api.py` 502) + the **freeze ruling** (`insert_observation` mints the `original` fact head — the sole vector home; `memories.embedding` no longer written; the embed-degradation signal moved to the live fact head) + the vector probe on the live fact head (`fetch_vector_candidates`) + wire deltas (`CorrectionResult` widened, `IngestResult.fact_version_id`) + REPL surfacing (`app\cli.py`) | floor-verifier **pass** against all six prior floors (read-path + write-path deliberately re-opened, re-verified): structural walker `tests\verify_fact_correction.py` re-run independently (32 assertions, every done-when criterion incl. db-layer distance-0 retrieval-follows-the-fix, the backfill guard via a legacy-shaped row, and all-or-nothing embed failure) on fresh scratch `longmem_test`; all five prior walkers re-run clean (38/38 — grown 35 → 38 incl. the one ruling-driven modification, the embed-signal query moved to the fact head; 36/36 read, 34 → 36 addition only; 33/33 authorial, 31 → 33 addition only; 42/42 reconstruction and 36/36 CLI harness **byte-untouched — the no-reconstruction-delta proof**, `app\retrieval.py`/`app\reconstruction.py` byte-identical to HEAD); migration 002 applied to `longmem`, no-arg migrate → **"Up to date: 2 applied, 0 pending"** (the criterion's new wording); `longmem` confirmed pristine via the postgres MCP (all product tables 0 rows, ledger = 001+002, old index absent, three fact indexes present); independent code spot-checks (one transaction with the embed outside it, CAS + broken-store rollbacks, sole DELETE still cache eviction, degraded path byte-identical); plus a live piped REPL beat: `:correct` prints both head swaps + embed timing, and the same query's relevance moved 0.4686 → 0.5637 across the correction | 2026-07-18 |
| Mid-dialogue gate v1 — **migration 003** (`db\migrations\003_fact_entities.sql`: `memory_fact_versions.entities` + guarded backfill + partial GIN on live fact heads + `memories_entities_gin` **dropped**, freeze ruled) + the gate stage (`app\gate.py` pure decision module: novelty + entity tripwire + damper, named signal constants; `app\retrieval.py` gated/loader branch — loader path v1-byte-parity, closed turns zero probe SQL, fires append `gate_fetch_k` new items via SQL-excluded probe or the GIN's entity-only rung) + the **entities freeze at observe** (`insert_observation` writes the fact head only) + the correction verb's NER + operator-field entities merge (`app\ingest.py`, `CorrectionNlpFailedError` → 502) + the fork-5 pre-serve callback (`app\reconstruction.py`, one defaulted param) + caller-held loaded-set/streak scene state (`app\session.py`) + the prompt recollection partition (`app\dialogue.py`) + `GateInstrumentation` wire deltas (`app\schemas.py`) + four knobs (`app\config.py`) + CLI gate line + load-driver `gate_check`/gate block | floor-verifier **pass** against all seven prior floors (write-path, retrieval, dialogue, session-runner, reconstruction, and both correction floors deliberately re-opened, re-verified): structural walker `tests\verify_gate.py` re-run independently (**51 assertions**, every done-when criterion incl. loader-parity, all ladder rungs, the blocking-callback beat, migration-003 legacy-row guard, and entities-follow-correction) on fresh scratch `longmem_test`; all seven prior walkers re-run clean, each on fresh scratch (40/40 write — 38 → 40 additive freeze pair; 36/36 read — **byte-untouched, the loader-parity proof**; 36/36 CLI harness — fixture pin + label edit only; 42/42 reconstruction — fixture pin only; 34/34 authorial — 33 → 34 additive; 34/34 fact — 32 → 34 additive); migration 003 applied to `longmem`, no-arg migrate → **"Up to date: 3 migration(s) applied, 0 pending"**; `longmem` pristine via the postgres MCP (ledger 001+002+003, all product tables 0 rows, new partial GIN present, old GIN absent); independent code spot-checks (loader path behavior-identical, callback-absent serve identical, one embed per turn, sole DELETE still cache eviction, no gate model role, reserved slots inert); plus the live piped REPL beat (loader → mid-scene novelty fetch → both-signal fire → `:correct` → **`(reconstructing…)` printed during the blocked turn**) and a standalone load-driver run with `gate_check` p50/p95 + the gate fire/efficacy block | 2026-07-19 |
| Encoding-context read term v1 + gate-calibration utility — the research-adoption slate's Target A (ruled 2026-07-20 with the plan; RaMem arXiv 2606.22844 + TARG 2511.09803 — `docs\research\CHANGES-FROM-RESEARCH.md` traces provenance): the formerly-reserved `DialogueInitRequest` context fields (`location_name`/`entities`/`event_time`) are **consumed** as client-supplied scene context — a soft multiplicative nudge `score ×= 1 + Σ w_i·match_i` (fact-head entity coverage, casefolded; `exp(−Δ/scale)` event-time kernel; casefold location match), **never a filter or penalty, ≥ 1 always**; a no-context request skips the term — **scoring byte-identical to v1** (the parity contract). Four knobs in `SERVICE_DEFAULTS` (per-agent overridable); applies on loader + gated (same context to loaded and fetched) + degraded paths; instrumentation-level surfacing (`context_active`/`context_components`) so the scored tuple and serving stage stay untouched. `CandidateRow`/`_CANDIDATE_COLUMNS` widened (+ `event_time`/`location_name`/`fact_entities`; degraded FROM gains a LEFT JOIN so legacy-shaped rows stay reachable); `DialogueTurnRequest` + session-runner scene context (scene-boundary reset) + REPL `:context`; `weight_overrides` **stays reserved**. A2: `--gate-budget` TARG-style calibration in the load driver — **report-only**, (1−rate)-quantile threshold recommendation off the run's novelty-distance CDF. **No migration** — schema frozen at 001+002+003. | floor-verifier **pass** against all eight prior floors (read-path floor deliberately re-opened — the one ruling-driven walker change): `tests\verify_read_path.py` re-run independently on fresh scratch (**42 assertions**, grown 36 → 42 — criterion [7] re-scoped from "reserved fields inert" to the context contract: no-context parity, exact ×1.75 / ×1.125 / ×1.0 factors casefolded both sides, factor-moves-score-only, never-echoed, weight_overrides-only still inert); the other six walkers **byte-identical to HEAD** and re-run green on fresh scratch (40/36/42/34/34/51); full suite **40 passed twice** (grown 38 → 40: Set B parity + exact factor, Set D gated-path factor with the gate decision proven untouched) + the keyless `-m "not nlp"` subset (33/33, API keys scrubbed in-process); `db\migrate.py` no-arg → "Up to date: 3 migration(s) applied, 0 pending"; `longmem` pristine via the postgres MCP (exact COUNT(*) on all ten product tables → 0; ledger exactly 001+002+003; no scratch residue); seven independent code spot-checks (context=None takes the v1 arithmetic path verbatim; `context_boost` pure, exactly 1.0 on no-match; same context to loaded+fetched; `gate.decide` inputs context-free; degraded LEFT JOIN proven with a live legacy-shaped row; `weight_overrides` consumer-free by grep; calibration writes nothing); plus a live piped REPL beat (stamped row score exactly ×1.75 under `:context entities=mara loc="the forge" time=<iso>`, bare row unchanged, `context: active (entities, event_time, location)` debug line, `:context clear`) and a standalone load-driver run with the `--gate-budget 0.3` calibration block *(A1 re-shape 2026-08-04: `weight_overrides` left the init request entirely — criterion [7] re-scoped again to assert the field's absence; the “weight_overrides-only still inert” / “consumer-free by grep” clauses describe the pre-A1 shape.)* | 2026-07-21 |
| Hybrid lexical retrieval channel v1 — the research-adoption slate's Target B (ruled 2026-07-20; survey 2604.01707 §7 + Engram 2606.09900): **migration 004** (`db\migrations\004_lexical_index.sql`, index-only — partial FTS GIN `to_tsvector('simple', basis_text)` over live fact heads, the 002/003 partial-index precedent) + the token-OR lexical candidate fetch (`lexical_tsquery`: casefolded ≥3-letter letter-runs, deduped, capped 16, OR-joined — the build-surfaced correction over websearch/plainto AND semantics, which would have made the channel inert; ts_rank + memory_id deterministic LIMIT) **unioned into the loader's vector over-fetch before scoring** — dedup by memory_id, scoring formula untouched, lexical hits carry their TRUE cosine distance (all-named params), NULL-embedding fact heads lexically reachable with relevance null (never a filter — exact-token recall softens the embed-degradation consequence). `lexical_fetch_k` knob (8.0; **0.0 = kill-switch**, pure-vector v1); `text_search_config` string knob ('simple' default baked into the index expression, the decay_classes plain-key precedent; overrides run unindexed, stated). Loader-scope v1 — the gate fire probe + entity-only rung are noted future consumers. Instrumentation `lexical_sql_ms` + `lexical_candidate_count`; CLI debug `lex=` field. | floor-verifier **pass** against all nine prior floors (read-path floor re-opened, re-verified; the gate walker's sole change is the mechanical ledger pin +004): `tests\verify_read_path.py` re-run independently on fresh scratch (**48 assertions**, 42 → 48 — new criterion [13]: lexical-only reach proven through the SERVED payload on a k=1/overfetch-1.0 agent, raw-2/union-2 exact dedup, unchanged formula, tokenless zero-SQL, kill-switch restores v1; criterion [9] **sharpened**: vector-path exclusion asserted on the probe itself + the honest lexical reach of an embed-degraded row); all seven walkers green on fresh scratch (40/48/36/42/34/34/51); migration 004 applied 001→004 in order on fresh scratch with an idempotent second run; **applied to `longmem`** — no-arg migrate → "Up to date: 4 migration(s) applied, 0 pending", index present with the exact expression + predicate (pg_indexes.indexdef); `longmem` pristine via the postgres MCP (exact COUNT(*) 0 on all ten product tables, ledger exactly 001..004, no scratch residue); full suite **41 passed twice** (40 → 41: the Set B lexical reach + kill-switch scenario) + keyless subset 34/34; floor economy by git-diff (reconstruction/gate/decay/ingest/dialogue/session/conftest and five walkers byte-identical to HEAD); seven independent code spot-checks (004 index-only; injection-safe tokenizer; additive-only union; v1-byte-identical when disabled/tokenless/gated/degraded; default-branch expression textually matches the index; all-named params; `_score_rows` zero hunks in this diff); plus the live REPL beat (k=1/overfetch-1.0: the pinned rare-name row served via lexical reach, `lex=2/3.63ms candidates=2 k=1`) | 2026-07-21 |
| Structural pytest suite v1 — `pytest.ini` (marker registration, `testpaths`, no cache residue) + `tests\conftest.py` (scratch **`longmem_suite`** session lifecycle: probe → create → migrate 001–003 → per-test TRUNCATE → drop; unreachable ⇒ loud skip, exit green; db-layer `InsertPlan` seeding with the pure fake embedding — the fast path never imports the NLP loaders; per-set configs with production-vs-fixture pins stated) + **38 scenarios** in five `test_*.py` files (Set A: authorial + fact chain incl. the db-layer distance-0 rank pair, CAS rollback, the pure constraint-follows-anchor test, and the marked route contract; Set B: decay-vs-invalidation incl. the two-time-travel-mechanics-agree and IDs-on-the-wire pairs; Set C: write-back chain shape, cache hit/frozen basis, band crossing, identity bump, correction eviction + re-anchor, drift refusal + refusal caching; Set D: loader parity, closed gate, novelty/tripwire/both fires, damper + reset, efficacy, runner append-only, marked entities-follow-correction; degradation: every ruled ladder row incl. the build-phase hard-stop, flagged as such in its docstring) + the Stop-hook subset edit (`-m "not nlp"`, ruled) + `pytest==9.1.1`/`httpx==0.28.1` pins | floor-verifier **pass** against all eight prior floors, standing by construction: `app\`, `db\`, and all seven walkers **byte-identical to HEAD** (the verifier's recorded git-diff proof — identical bytes need no re-run); full suite re-run twice independently (38/38, 38/38 — the determinism criterion), the subset re-run with API keys scrubbed (31/31 in ~14 s — the keyless + no-spaCy proof), the unreachable-skip beat (30 skipped + the one pure no-DB test, exit 0, loud warning), the hook contract both ways (green → exit 0; `stop_hook_active` guard short-circuits; red path + dormant guards read intact with the subset flag), a structural-only audit of all five files (no assertion touches model prose), no-arg migrate → "Up to date: 3 migration(s) applied, 0 pending", `longmem` pristine via the postgres MCP (ten product tables 0 rows, ledger 001+002+003, **no `longmem_suite` residue**), and pins == installed versions | 2026-07-20 |
| Split-brain streaming v1 — the dialogue seam re-shaped into two concurrent calls off one retrieval (`split-brain-streaming.md`, ruled topology; the reserved `WeightOverrides` slot goes live for the behavior view): the streaming **prose** call (`app\providers.py` `DialogueProvider.stream_prose` — a sync generator yielding chunks + a `ProseResult`; real streams raw text, no JSON) + the concurrent **behavior** call (`BehaviorProvider.decide` — directive + delta JSON, new `LONGMEM_MODEL_BEHAVIOR` role + `LONGMEM_PRICE_BEHAVIOR_IN/OUT`, hardened parse) + the async-generator seam (`app\dialogue.py` `run_dialogue_turn`: retrieval once → two views → concurrent legs via a worker-thread + `asyncio.Queue` bridge → validate/apply → terminal `DialogueTurnResult`; prompt split `assemble_prose_prompt`/`assemble_behavior_prompt` with a recent-actions block, prose prompt only; behavior view = `rank_behavior_view` exponent-form re-rank of the served set, `resolve_behavior_weights` clamp `[0,4]`) + caller-held recent-actions scene state + `stream_utterance`/`utterance` (`app\session.py`) + REPL live streaming + divergence debug view (`app\cli.py`) + `first_word`/`behavior` series + behavior cost row (`app\load_driver.py`) + wire deltas (`app\schemas.py`: `first_word_ms`/`prose_stream_ms`/`behavior_ms`/behavior tokens; `dialogue_view`/`behavior_view` `ScoredRef` divergence record; `RecentAction`; `weight_overrides` live on `DialogueTurnRequest`). No migration | verified inline this session against all eleven prior floors: **CLI-harness walker re-opened `tests\verify_cli_harness.py` (36 → 55 assertions** incl. concurrency proved with a slow behavior fake — first chunk 0.008 s vs behavior 300 ms; dialogue-view parity at 1.0 + exponent re-rank flip on crafted items; the divergence record; the recent-actions block appears iff scene actions and clears at `:scene`; all four degradation rows) on fresh scratch; read-path walker weight_overrides criterion re-scoped (48); gate walker `assemble_system_prompt` → `assemble_prose_prompt` rename-only (51); write/reconstruction/authorial/fact walkers **byte-identical to HEAD** and green (40/42/34/34); full suite **41 → 42 passed twice** (+ the split-brain divergence/parity scenario) + keyless subset 35; no-arg migrate → "Up to date: 4 migration(s) applied, 0 pending"; `longmem` pristine via the postgres MCP (ten product tables 0 rows, ledger 001–004, no scratch residue); live piped REPL streaming beat + a standalone driver run. **floor-verifier pass** (independent, fresh context — re-ran all ten done-when + all seven walkers on fresh scratch 55/48/51/40/42/34/34, full suite 42 ×2 + keyless subset 35, no-arg migrate "4 applied, 0 pending", `longmem` pristine via the postgres MCP, the ten untouched app files + four untouched walkers confirmed byte-identical to HEAD~1 by git-diff, no invariant violated — sole persisted write is the sanctioned `agents.reputation` UPDATE). *(SUPERSEDED by the A1 re-shape 2026-08-04 — the behavior call, action directive, divergence record, recent-actions block, and reputation apply this row lands were removed by ruling; the weights machinery survives, re-pointed at the prose view (weights-on-speech). After A1 a dialogue turn persists NOTHING — the “sole persisted write” clause describes the retired shape. `split-brain-streaming.md` carries the retirement banner; `architecture.md` §9 is the living seam.)* | 2026-07-21 |
| Escalation soft-degrade v1 — **migration 005** (`db\migrations\005_escalation_failed.sql`: `memories.escalation_failed boolean NOT NULL DEFAULT false`, mirroring `scoring_failed`) + the observe-path soft-degrade (`app\ingest.py` `_escalate_with_retry` returns `None` on double failure instead of raising; the write proceeds with the base NLP-pass gist and sets `escalation_failed`; `EscalationHardStopError` + its observe-route 502 removed) + the flag on `InsertPlan` → the new column and `IngestResult` (`app\db.py`, `app\schemas.py`) + the `app\cli.py` consumer removed (import + REPL `except`, build-surfaced by the floor-verifier). Retires the 2026-07-13 fail-loud hard-stop (ruled 2026-07-22); the correction verb's own fail-loud embed/NER → 502 paths untouched. The trigger-set/threshold tuning (79%-fire) stays a separate open item. | floor-verifier **pass** (write-path floor re-opened, re-verified): `tests\verify_write_path.py` **42** on fresh scratch with the flipped [11] soft-degrade (write lands + `escalation_failed` True on the result AND the column); the two build-surfaced breakages fixed and re-verified — `verify_cli_harness.py` **55** (`app\cli.py` import/except removed) and `verify_gate.py` **51** (mechanical ledger pin +005, the 004 precedent); read/reconstruction/authorial/fact walkers green (48/42/34/34); full suite **42** + keyless subset **35**; migration 005 applied 001→005 on fresh scratch + idempotent second run + column DDL `boolean NOT NULL DEFAULT false`; **applied to `longmem`** (no-arg migrate → "5 migration(s) applied, 0 pending"); `longmem` pristine via the postgres MCP (ten product tables 0 rows, ledger 001–005, no scratch residue); invariants intact (non-destructive proceed — a row lands flagged, never a lost write; no in-place content UPDATE; sole DELETE remains the correction cache eviction). | 2026-07-22 |
| HTTP dialogue-turn route + perceived-TTFT v1 — the Unity/C# front door (audit-replan item 1): `POST /v1/dialogue/turn` (`app\api.py`; `DialogueService` joins the lifespan) drains `run_dialogue_turn`'s async generator to the terminal `DialogueTurnResult` — **stateless** (scene state caller-held on the request; runner bookkeeping stays client-side — the future C# `NpcSession` ports `_apply_turn_result`), non-streaming, `UnknownAgentError` → 404 / `UnknownIdentityVersionError` → 422 (the existing precedents), pass-through by ruling, `on_reconstruct=None` (no during-wait signal without SSE; the post-hoc fields carry it); a future SSE `/turn/stream` iterates the SAME generator + the honest latency metric `perceived_first_word_ms` (`app\schemas.py`; captured in `app\dialogue.py` at the same first-chunk instant as `first_word_ms`, clocked from `t_total` — retrieval-inclusive, sees the cold-reconstruction stall; 0.0 when no chunk arrives; the <1 s bar's field) surfaced in the CLI debug line (`app\cli.py`) + the `perceived_first_word` driver series (`app\load_driver.py`). Thread-pool cap ruled deferred post-demo. No migration; no new knobs or roles. | floor-verifier **pass** against all twelve prior floors (CLI-harness floor re-opened, re-verified): `tests\verify_cli_harness.py` **62** on fresh scratch (55 → 62 — new section [13]: route JSON == the drained seam result via ASGITransport + a capturing wrapper, 404 + 422; perceived > first_word > 0 in [1]; both-TTFT-zero on the pre-chunk-failure row in [9]; the driver series in [12]); six other walkers green on fresh scratch (48/51/42/42/34/34) and — with every other `app\` file, `conftest`, and the four other suite files — **byte-identical to HEAD by git-diff** (the floor-economy proof); full suite **43 ×2** + keyless subset **36** (the new unmarked Set D route-contract scenario); no-arg migrate → "Up to date: 5 migration(s) applied, 0 pending"; `longmem` pristine via the postgres MCP (ten product tables 0 rows, ledger 001–005, no scratch residue); independent code spot-checks (route adds/drops nothing; stateless — the sole persisted write of a turn remains the sanctioned `agents.reputation` UPDATE inside the seam; both TTFT fields one-instant captured from `t_prose` vs `t_total`; structural-only test additions); plus a live `python -m app.serve` HTTP beat (observe → turn with IDs + scores + both views + both TTFT fields → unknown-agent 404) and a standalone driver run emitting the `perceived_first_word` series. *(A1 re-shape 2026-08-04: the “sole persisted write … `agents.reputation` UPDATE” clause describes the retired seam — a turn now persists nothing; the route itself stands and was re-verified against the re-shaped seam.)* | 2026-07-23 |
| Escalation thin_gist trigger v1 — the trigger-tuning ruling's build (same day as the turn route; the dated "Escalation trigger tuning" entry in `decisions.md` carries the full measurement): a **sixth escalation trigger `thin_gist`** (`app\nlp.py` `evaluate_triggers`, appended LAST — the biased-loose add-only contract preserved) fires when the base NLP pass yields fewer gist spans than `escalation_min_base_spans` (new `SERVICE_DEFAULTS` knob, default 1.0 = fire on a zero-span base pass; 0.0 disables; per-agent overridable, fetched with the other escalation knobs in `app\ingest.py`). Closes the measured **zero-gist hole** — 16/80 realistic observes otherwise landed with NO gist spans, leaving reconstruction's fixed constraint empty on those rows (75% → 95% fire on the corpus, ~+$0.03/100 observes). The five original triggers + thresholds stand unchanged by ruling. No migration; no new model roles. | floor-verifier **pass** against all thirteen prior floors (write-path floor re-opened, re-verified): `tests\verify_write_path.py` **46** on fresh scratch (42 → 46 — [11b]: thin_gist fires ALONE on a zero-span pass at production knobs, the 0.0 kill-switch, floor-compares-the-COUNT, and the measured sexton fixture escalating with thin_gist as the SOLE trigger at the service level); six other walkers green on fresh scratch (62/48/51/42/34/34) and — with every other `app\` file — byte-identical to HEAD by git accounting; full suite **44 ×2** + keyless subset **37** (the new pure `test_thin_gist_trigger_pure` asserts over production `SERVICE_DEFAULTS`, no DB/NLP — it rides the Stop-hook subset); no-arg migrate → "Up to date: 5 migration(s) applied, 0 pending"; `longmem` pristine via the postgres MCP (ten product tables 0 rows, ledger 001–005, no scratch residue); code spot-checks (trigger add-only and appended last, the knob's floor value lives only in `SERVICE_DEFAULTS`, the 2026-07-22 soft-degrade path untouched and re-proved live, no new writes/UPDATE/DELETE); the predicted fake-mode consequence verified, not assumed (stored fixture rows byte-identical — only escalated/escalated_by/timing moved; no foreign assertion touched). | 2026-07-23 |
| Unity-client stage 0 v1 — the C# client's backend surface (unity-client.md forks 1–3, ruled 2026-07-27): **SSE `POST /v1/dialogue/turn/stream`** (`app\api.py` — iterates the SAME `run_dialogue_turn` async generator via a queue-bridged pump task; `chunk` events JSON-encoded, an optional `reconstructing` event off the pre-serve callback, terminal `result` = the seam result's serialization; the FIRST queue item awaited before the response starts so unknown-agent/version still map 404/422; in-stream failure → an `error` event; a task-reference set so a client disconnect never aborts the turn server-side) + **`POST /v1/agents`** (`db.insert_agent` + `IngestService.create_agent` + `CreateAgentRequest/Result` — server-minted UUID, exact-fields storage, NULL knobs resolve config → `SERVICE_DEFAULTS`; the build's only new write surface, an agents row outside the memory-content invariant's subject) + the **unscored inspector reads** (`GET /v1/memories/{id}/chain` + `GET /v1/agents/{id}/memories` — `db.fetch_memory_chain`/`fetch_agent_memories` + `RetrievalService.memory_chain`/`agent_memories` + chain/index wire models: both version chains ordered (valid_at, created_at) with superseded rows PRESENT, gist spans, `has_embedding` never the vector, `memories.entities` deliberately not echoed, the index LEFT JOINed to the live telling head, `limit` a caller arg 1–1000). No migration (ledger 001–005); no new knobs or model roles. | floor-verifier **pass** against all fourteen prior floors (write-path + read-path + CLI-harness floors re-opened, re-verified): `tests\verify_write_path.py` **51** on fresh scratch (46 → 51 — [15] provisioning: server-minted row with exact fields + NULL knobs, pass-through echo, the provisioned agent immediately a working write target, empty name 422); `tests\verify_read_path.py` **56** (48 → 56 — [14] inspector reads: superseded telling row PRESENT with coherent timeline after a db-layer correction, fact chain with `has_embedding` + entities, index newest-first beside the LIVE head, limit-vs-total_count, both 404s); `tests\verify_cli_harness.py` **67** (62 → 67 — [14] SSE: 200 text/event-stream, chunk-event count == the seam's, chunks concatenate BYTE-IDENTICALLY to the result's content, result event == the seam serialization, pre-stream 404); four untouched walkers green on fresh scratch (51/42/34/34) and byte-identical to HEAD by git diff; suite **48 ×2** + keyless subset **41** (four new unmarked route-contract scenarios in `test_set_d_gate.py`); no-arg migrate → "Up to date: 5 migration(s) applied, 0 pending"; `longmem` pristine via the postgres MCP (ledger 001–005, ten product tables 0 rows, no scratch residue); invariants (the only new write is the agents INSERT; inspector reads SELECT-only end to end; the turn's sole persisted write remains the sanctioned reputation UPDATE — `app\dialogue.py` byte-identical); floor economy exactly nine files. *(A1 re-shape 2026-08-04: the “turn's sole persisted write remains the sanctioned reputation UPDATE” clause describes the retired seam — a turn now persists nothing; provisioning stopped accepting/writing the reputation fields.)* | 2026-07-27 |
| Unity-client stage 1 v1 — `NpcMemory.Core` + the console harness, the Wk-1 interop go/no-go (unity-client.md ruled shape; the project's first C#): `client\NpcMemory.Core\` (**netstandard2.1** — the Unity 6 compatibility profile; sole dependency Newtonsoft.Json 13.0.3; **zero `UnityEngine` types** — the engine-agnostic ruling) holding `NpcJson` (ONE serializer config: SnakeCaseNamingStrategy + `NullValueHandling.Include` + `DateParseHandling.None`/`DateTimeOffset` end-to-end — the null-vs-`[]` tri-state contract is load-bearing and never collapsed), `Models.cs` (a field-for-field mirror of every `app\schemas.py` wire model), the flat `NpcMemoryClient` (all ten routes 1:1, per-route settable timeouts, typed loud `NpcMemoryApiException`, the SSE consumer with chunk/reconstructing/result dispatch), and `NpcSession` (the field-for-field `_apply_turn_result` port: loader-seeds/gated-append/closed-untouched keyed on the SERVER's gate record; recent-actions cap; boundary resets; `as_of` time travel; boundary snapshot refresh from the last `reputation_after` — the documented single-client HTTP equivalence) + `client\NpcMemory.Harness\` (net8.0 console playing every demo beat headless). | verified by the live interop gate + independent floor-verifier **pass**: `dotnet build` 0 warnings; the harness ran **21/21 checks green** against a live `python -m app.serve` (fake mode, scratch `longmem_smoke`, port confirmed then dropped): provisioning, time-travel observes, the tri-state proof (null → loader / populated → evaluated / **explicit `[]` → evaluated**), SSE 7 chunks concatenating byte-identically to content with perceived > first_word > 0, correction both-head swap + chain (superseded PRESENT, coherent timeline) + index (live head, detail_count 2), 46-day reconstruction (write_backs 3) + byte-identical within-scene cache-hit reread, mid-scene gate fire (novelty) appending the unseen memory, warm-init → on-camera pure cache hit (0 write-backs, 4 hits), both scored views over one served set; verifier side-by-side port-fidelity review vs `app\session.py`; no SQL and no DB driver in `client\` (dependency closure exactly Newtonsoft); nothing hardcoded beyond documented overridable defaults; `git diff` empty vs HEAD `b490867` (the stage-0 floor byte-untouched); keyless subset re-run 41; `longmem` pristine via the postgres MCP. *(A1 re-shape 2026-08-04: `NpcSession`'s snapshot refresh, recent-actions cap, and directive/reputation surface were removed; the harness gate was re-composed — still 24 checks, the divergence + reputation-callback checks replaced by the weights-on-speech parity/re-rank pair — and re-run 24/24 live.)* | 2026-07-27 |
| Unity-client stage 2 v1 — the Unity adapter + gray-box set (unity-client.md stage 2, the demo vehicle; ruled 2026-07-27): a thin `MonoBehaviour` adapter `NpcMemoryNpc` (`unity\Assets\Scripts\NpcMemoryNpc.cs` — wraps `NpcMemory.Core`'s `NpcSession`/`NpcMemoryClient`; `baseUrl` default `http://127.0.0.1:8000`, `autoProvision`, directive + reputation callbacks marshaled to the main thread by the no-`ConfigureAwait(false)` core) + the in-scene verification harness `NpcDemoDriver` (`unity\Assets\Scripts\NpcDemoDriver.cs`; `autoRun` plays 8 scripted beats — provision → injected-time observes → scene boundary → loader turn → streamed turn — logging `[npc-demo]` receipts; `Application.runInBackground` so frames pump while unfocused) over the gray-box set (`unity\Assets\Scenes\SampleScene.unity`: primitive room + Keeper capsule + IMGUI dev-tool overlay, the ruled systems aesthetic); the committed core rides as the plugin DLL `unity\Assets\Plugins\NpcMemory\NpcMemory.Core.dll`. No backend change (ledger 001–005); no new knobs or model roles. | floor-verifier **pass** against the stage-1 console-interop floor (same `NpcMemory.Core`, proven byte-identical): the Play-mode gate ran **8/8 GREEN twice** through the MCP-for-Unity bridge — main-loop-driven (fake mode, scratch `longmem_smoke`, agent 552052c0, +11 frames) then **independently re-driven by the floor-verifier** (fresh agent 1bbd6b3b, +12 frames, 0 console errors) — all 8: in-engine provisioning, injected-time observes, boundary froze identity+basis, loader turn served IDs+scores (2 items), streamed chunks byte-identical to content, **chunk callbacks on the main thread (thread 1)**, second turn gated, frames pumped through both turns; DLL build-identity provenance re-proven — a fresh `dotnet build -c Release` differs from the committed DLL by **150 bytes, ALL build-identity** (COFF stamp, MVID, PDB GUID/checksum, embedded version sha), fresh sha == HEAD `5bec081`, all IL/metadata byte-identical → no source/binary drift (the committed DLL embeds ancestor `ae967cf`, built 5 min before `750a9dd`, IL current); plus a **real-mode corroboration pass** (Haiku dialogue, real config validated at startup, fresh scratch DB) that also went **8/8** — check #8 the regression test fake mode cannot be: **+1061 frames** through the real streamed turn (vs +11/+12 fake) proving the main thread never blocked between real inter-chunk gaps (the SSE `ConfigureAwait(false)`-removal / `ReadLineAsync` fix holds under real streaming); `longmem` pristine (only `longmem_smoke`, created/migrated/dropped — `databases present: ['longmem']`); no invariant touched (adapter calls only sanctioned verbs; scene NOT saved — committed `autoRun: 0` preserved). Two non-blocking soft spots: check #1 is a guarded `Check(true,…)` (the 30 s Ready-wait timeout is the real gate) and directive/reputation callbacks are subscribed-but-not-asserted (delegated to the stage-1 floor). *(Re-opened by the A1 re-shape 2026-08-04 — the adapter's directive/reputation surface and the driver's flash/readout were removed, and the plugin DLL was rebuilt + refreshed. The 8/8 beats assert nothing behavior/reputation-shaped, so compile + re-run is the remaining proof; **re-run pending the next editor session** — the MCP bridge needs the Editor open before session start, and it was not this session. **Resolved 2026-08-05:** re-run **8/8 GREEN** fake-mode through the bridge (Editor open before session start; agent 594b2a18, +13 frames through both turns, 0 console errors/warnings — the shrunk adapter compiled clean, no `ActionDirective` references; scratch `longmem_smoke` created/migrated/dropped, `longmem` pristine, scene NOT saved — committed `autoRun: 0` preserved). Fake-only by ruling 2026-08-05: the real pass's unique evidence (frame-count through real SSE gaps) tested streaming code A1 didn't change, and the committed DLL is sha256-identical to a fresh build.)* | 2026-07-29 |
| Eval-harness stage 1 v1 — the judge-free metric layer + the reconstruction-metrics route + The Ledger binding (`eval-harness.md` stage 1, plan-as-spec session; the four plan-time rulings dated in `decisions.md`): `app\eval_metrics.py` (pure metric arithmetic — anchor-cause-aware gist facts via the constraint's own `merge_spans`/`split_gist_detail`, the threshold presence rule, **honest-None denominators** — never a flattering 1.0, whole-word fabrication grounding, keyword retention, the composed-cache-key band parser) + `nlp.lemma_content_set` (the write-pass token filter promoted public) + the `metric_gist_match_threshold` knob (1.0, agent_knob contract) + `ReconstructionMetricsResult` (18 fields) + read-only `db.fetch_cache_keys` + `RetrievalService.reconstruction_metrics` (live-head-only scoring, fork 6; the PURE identity render — zero writes) + `GET /v1/memories/{id}/reconstruction-metrics` (404 the /chain shape; the **third unscored-by-contract read** — CLAUDE.md carve-out grown) + the Ledger numbers-panel binding (inside try/catch — a metrics failure never hides the chain) + the C# mirror (`Models.cs` 31 → 32 wire models, `ReconstructionMetricsAsync`, harness gate 23 → 24) + suite **Set G** (`tests\test_eval_metrics.py`: 5 pure unmarked + 3 nlp-marked). **No migration (ruled — ledger 001–005)**; no new model roles (the judge role arrives with stage 3, eval-runner-only by ruling). | floor-verifier **pass** against the inspector-reads/route floor (2026-07-27) and the reconstruction serving floor (2026-07-17), both re-proven intact: full suite **63** (55 → 63, Set G) + keyless subset collects **53**; `verify_read_path.py` **56** and `verify_reconstruction.py` **42** re-run green on `longmem_test`; ruff format + check clean at the pin (0.15.21); both C# builds 0 warnings; the live console interop gate **24/24** against a served fake-mode backend on fresh `longmem_smoke` — the new check exercised the honest-denominator path live (gist 0/0 → precision null, bands `[3]`); the route contract live (18-key 200 payloads on all four harness memories; 404 on a random UUID; ratios null exactly when denominators are empty; the corrected memory: `anchor_cause` correction + `detail_recall` null); the **zero-write proof three ways** (the Set G non-perturbation scenario re-run by node id; grep — no insert_/ensure_/write_back call sites in the method, `fetch_cache_keys` SELECT-only; live seven-table counts byte-stable across four metrics reads with `/chain` identical modulo the per-call `total_ms`); teardown clean (server stopped, `longmem_smoke` dropped, `longmem` untouched — harness agent 0 rows there, ledger 001–005 intact); main-loop corroboration the verifier scoped out: the Ledger rendered **gist precision 1.00 (1/1 facts held)** on the corrected-then-reconstructed chain in a real browser, chain fully visible. *(A1 re-shape 2026-08-04: the “harness gate 23 → 24” C#-mirror clause predates the A1 re-composition — the gate remains 24 checks with the weights-on-speech pair in place of the divergence/reputation checks.)* | 2026-07-29 |
| A1 re-shape v1 — split-brain removal + weights-on-speech (ruled 2026-08-04, ruling 2 of the scope-consolidation entry; the four spec forks + seam-design contracts in the dated A1 entry): the dialogue seam re-shaped to ONE streaming prose call (`app\dialogue.py` — the behavior call + `behavior` model role (real mode 7 → 6 vars), the action directive, the divergence record, the recent-actions channel, and the reputation system removed end to end; `apply_reputation_delta` deleted; provisioning stops accepting/writing `reputation`/`reputation_sensitivity`; `DialogueInitRequest.weight_overrides` removed) + **weights-on-speech** (`resolve_dialogue_weights`/`weighted_score`/`rank_dialogue_view` — the split-brain exponent re-rank re-pointed at the served view feeding the PROSE prompt; resolution request → `agents.config` `weight_*` → 1.0, clamp `WEIGHT_MIN/MAX` [0,4]; parity at all-1.0 on loader turns — `dialogue_view` == the (id, score) projection of `items`; membership never changes) + the C# mirror (`NpcMemory.Core` shrunk field-for-field; `NpcSession(client, agentId, phaseTag)`; harness beat [10] re-composed to the parity/re-rank pair) + the Unity adapter shrink + the Ledger single-view turn panel. **A dialogue turn persists nothing.** Zero schema change (ledger 001–005). | floor-verifier **pass** (independent, fresh context — re-ran everything itself): ruff check + `format --check` clean; migrate "5 applied, 0 pending" on scratch; all seven walkers green on `longmem_test` (write 53, read 56 — criterion [7] asserts the init field's absence, CLI **51** — the rewritten weights-on-speech walker, gate 51 — three `assemble_prose_prompt` call sites on the new signature, reconstruction 42, authorial 34, fact 34) + `db\smoke_test.py` rolled back clean; full suite **63** + keyless subset **53**; both C# builds 0 warnings; the live console interop gate **24/24** against a served fake-mode backend — beat [10a] parity + [10b] `Recency = 0.0` re-rank (same served set, new order) confirmed live; floor economy — `app\retrieval.py`/`app\gate.py`/`app\eval_metrics.py`/`app\reconstruction.py`/`db\migrations\` **byte-identical to HEAD** by git diff; zero-persistence proven by inspection (the sole in-place scalar UPDATE is `set_pinned`, the sole DELETE the correction cache eviction) and by the walker's memories-count + `agents.reputation IS NULL` asserts; `longmem` pristine (ten product tables 0 rows, ledger exactly 001–005); config surface exactly six roles + `weight_*` knobs. **Pending:** the Unity Play-mode gate re-run — the Editor was not open before session start so no `mcp__UnityMCP` tools registered (the known ordering constraint); the committed plugin DLL is **sha256-identical to a fresh Release build of the tree** (the verifier's provenance proof), so the outstanding proof is the in-engine compile + 8/8 re-run at the next editor session (the stage-2 row carries the dated re-open note). **Resolved 2026-08-05** — fake-mode 8/8 GREEN + clean compile; evidence in the stage-2 row's dated resolution note (fake-only by ruling). | 2026-08-04 |
| Eval-harness stage 2 v1 — the runner core (`eval-harness.md` stage 2, built to the contract paragraph verbatim; session rulings + build latitude in the dated 2026-08-05 `decisions.md` entry): `app\scratch_db.py` (the verified `tests\scratch_uri.py` rewrite PROMOTED — the shim re-exports it so conftest + all seven walkers stay byte-untouched — plus `provision_scratch`/`drop_scratch`: pid-scoped names, double product-DB refusal (name check + conninfo re-parse proof), create + `db\migrate.py` subprocess + forced drop) + `app\eval_scenarios.py` (strict pydantic scenario schema — the seven REPL-parity event kinds, `extra="forbid"`, `memory_ref` = observe ordinal validated backward-only at load, tz-aware datetimes — plus the ONE JSONL loader serving scenarios AND drift corpora via `assert_corpus_shape` (fork 10) + membership-only `check_expected` (ruled)) + `app\eval_runner.py` (`python -m app.eval_runner`; `run`: scratch provision → per-scenario `db.insert_agent` → literal event replay through `SessionRunner` → expected-IDs vs the raw `items` echo → per-memory metrics via the ONE assembly `RetrievalService.reconstruction_metrics` → honest-denominator summary + latency p50/p95 + run-total tokens with USD-optional → artifact JSON under gitignored `data\eval\runs\` (fork 7) + stdout tables, exit 0/1; `drift-validate`: real-mode-gated with `--plumbing` ⇒ `plumbing_only: true` (ruled), age-jump + authored basis re-freeze, per-item distances via the capture seam, p50/p95/max + over-budget + the `drift_refusals` self-check, exit 0/1/2) + the `drift_observer` capture seam in `app\reconstruction.py` (module attribute `Callable[[UUID, float, bool], None] \| None = None` fired between the distance computation and the refusal branch — the whole diff is two hunks) + fixtures (`data\eval\scenarios\smoke.jsonl`: 5 authored-prose scenarios covering all seven event kinds incl. one held-out, scoring pinned by explicit config facts; `data\eval\corpora\drift-fixture.jsonl`: 7 observes) + suite **Set H** (`tests\test_eval_runner.py`: 8 unmarked + 1 nlp). **No migration (ruled — ledger 001–005); nothing eval persists in Postgres.** | floor-verifier **pass** (independent, re-ran everything itself): ruff format + check clean; full suite **72** (63 → 72, Set H) + keyless subset **61**; `verify_reconstruction.py` **42** re-run green — the byte-identity proof for the None-default seam, the diff confirmed exactly two hunks — and `verify_read_path.py` **56** (the shim smoke) on `longmem_test`; fake e2e `run` exit 0 with **6/6 checks twice, byte-identical outcomes** (held-out excluded by default, artifact confirmed gitignored by `git check-ignore`); plumbing `drift-validate` **7/7 checked, 0 over budget**, self-check matches, and fake mode WITHOUT `--plumbing` refused exit 2 on stderr; the product-DB refusal raised instantly against a TEST-NET host (no dial-out); live `longmem` pristine (ten product tables 0 rows, ledger exactly 001–005, no `longmem_eval_*` left behind); the four DML tokens absent from all three new modules; the ruled single real-mode run's artifact read and confirmed — **7 items, 0 over budget, distance p50 0.030 / p95 0.100 / max 0.120**, self-check match true. | 2026-08-05 |
| Eval-harness stage 3 v1 — the judge layer (`eval-harness.md` stage 3, built to the contract paragraph with the one dated API-forced correction — adaptive thinking / no sampling params in place of the unimplementable "temperature 0"; session rulings + build latitude in the dated 2026-08-07 `decisions.md` entry): config (`LONGMEM_MODEL_JUDGE` **loaded in both modes, required by neither** — the 2026-07-29 eval-runner-only ruling as Set I's regression test; `LONGMEM_PRICE_JUDGE_IN/OUT`; `LONGMEM_JUDGE_MAX_TOKENS` default 2048; the `LONGMEM_DIALOGUE_THINKING` knob — `""` omits the parameter, the pre-B2 prose request byte-for-byte, `"disabled"` = the sonnet-5 thinking-off arm; all three on the override allowlist) + providers (`FakeJudgeProvider` deterministic hash verdicts; `RealJudgeProvider` Opus-4.8-class by ruling, adaptive thinking, `MalformedOutputError` with token accounting; Failing/Malformed fakes; standalone `build_judge_provider` — the judge stays OFF the frozen `Providers` bundle) + the pure `app\eval_judge.py` (four versioned rubrics sf-v1/abst-v1/rf-v1/pp-v1, pydantic verdicts, position-swap combine with disagreement⇒tie, hand-rolled Cohen's kappa with honest-None, Pareto non-domination, blind gold rows) + the runner's judged pass (prose capture judged-only — the plain `run` artifact byte-untouched; per-item `judge_failed` degradation; judge ms/tokens/USD at the seam; the `models` provenance stamp) + three verbs (`compare`: arm-overlay JSON files with mode/db/keys/judge refused and per-arm dialogue prices — the cross-model USD caveat by construction; two pid-scoped scratch DBs; pairwise prose position-swapped; the accuracy/p50/p95/USD-per-100-turns Pareto table with non-dominated marks; `emit-gold`: blind candidates, verdicts stripped, judge_failed skipped, per-category cap 30; `agreement`: raw % + kappa per category vs `--kappa-bar` 0.6, undefined kappa honestly fails) + `JudgedSpec` on `UtteranceStep` (additive, strict) + fixtures (`judged.jsonl`: 8 scenarios, 24 sf + 24 abstention incl. a true-premise control per trio, aged probes forcing reconstruction; three committed arm files incl. `sonnet5-thinking-off`) + suite **Set I** (13 unmarked + 1 nlp). **No migration (ledger 001–005); nothing eval persists in Postgres.** | floor-verifier **pass** (independent, 12/12 — re-ran everything itself, every offline command under the fake override): ruff check + `format --check` clean (43 files); full suite **86** (72 → 86, Set I) + keyless subset **74**; four walkers re-run green on fresh scratch (write **53**, read **56**, reconstruction **42**, CLI-harness **51**) with the gate/authorial/fact walkers byte-identical to HEAD by git diff (floor economy — the build touched only the five app files, Set I, fixtures, and docs); fake e2e ×2 → **6/6 checks each, identical modulo timestamps/db-name/UUIDs/latencies**, artifact carrying NO judged/plumbing/prose keys and no judge cost row (the no-judged-path-untouched proof; the one all-runs addition is the `models` provenance block); the `--judged` fake-mode gate refused **exit 2 BEFORE provisioning** (code-confirmed ordering) and `--plumbing` ran the full judged mechanics (24 sf + 24 abstention + 27 faithfulness items, `plumbing_only: true`, judge cost row present); compare plumbing exit 0 with two stamped arm blocks (arm b `dialogue_thinking: "disabled"`), the 6-pair pairwise block, and Pareto non-dominated flags; `agreement` exercised three ways (perfect labels → kappa 1.0 exit 0; flipped labels → kappa 0.0270 exit 1; degenerate single-class → "undefined" exit 1); the ruled single REAL judged smoke's artifact re-read and every number confirmed — 12 scenarios / 62 turns / 44 observes / 0 degraded, checks 6/0, `models.judge` claude-opus-4-8, sf 18/24, abstention 23/24, faithfulness 88/89 facts + 63 fabricated-claim flags, **0 judge_failed**, 7 skipped-not-reconstructed, judge 36784/8574 tokens $0.39827; the gold file **78 blind rows** (24/24/30, every label null, no verdict key in the file); `longmem` pristine via the postgres MCP (ten product tables 0 rows, ledger exactly 001–005, no `longmem_eval_*` residue); zero SQL DML tokens in `app\eval_judge.py` and the runner's writes confined to `db.insert_agent`/`SessionRunner`; judged outcomes proven unable to move exit codes (structural-checks-only 0/1, 2 = gate). | 2026-08-07 |
| Eval-harness stage 4 v1 — the fixed-gist ablation (`eval-harness.md` stage 4, built to the contract paragraph; the labeling-workaround rulings incl. fork 11 = exclude, and the build record, in the dated 2026-08-12 `decisions.md` entries): config (`SERVICE_DEFAULTS["reconstruction_gist_constraint"] = 1.0` — the gate_enabled kill-switch shape, agent_knob contract, production byte-identical at default) + `app\reconstruction.py` (`_SYSTEM_TASK_NO_GIST`; `build_reconstruction_item(..., gist_constraint=)` — False blanks the gist on ORIGINAL-anchored items only, a correction-anchored chain keeps the corrected head regardless (fork 11); `assemble_reconstruction_prompt(..., include_gist_constraint=)` — False swaps the task constant and omits the `gist` key; serve() reads the knob and partitions call slots — knob OFF = a no-gist group + a normal correction-anchored group; the knob deliberately NOT in `compose_cache_key`) + the drift-validate replay core extracted as `replay_aged_probe` (shared, behavior preserved) + the `ablation` verb (two pid-scoped scratch DBs, arm B's agent config carries the 0 knob via `insert_agent` — no runner signature change; per-item drift via the observer seam + the judge-free stage-1 metric read; arms paired on (scenario_id, memory_ref) with both UUIDs as provenance; mean paired \|Δ\| beside per-arm gist-precision/fabrication; exit 0 = all paired + drift-checked both arms / 1 otherwise / 2 = the real-mode gate) + `data\eval\corpora\ablation-fixture.jsonl` (3 scenarios × 8 observes, observe/as_of only, both decay classes pinned 2160000.0 + `decay_k_importance` 0.0 for cross-arm band determinism) + suite **Set J** (`tests\test_set_j_ablation.py`: 5 unmarked + 1 nlp two-arm plumbing e2e). **No migration (ledger 001–005); judge-free by design; nothing eval persists in Postgres.** | floor-verifier **pass** (independent, fresh context — re-ran everything itself, 7/7 checks): non-nlp suite **81**; Set J **6/6** incl. the nlp e2e; the stage-1–3 eval floor re-run fully (`test_eval_metrics` + `test_eval_runner` + `test_set_i_judge` = **33 passed** — the replay-core extraction holds under the stage-2 tests); `verify_reconstruction.py` **42** green (on `longmem_test`, recreated + migrated 001–005 this session after being found missing — the pool-timeout mask diagnosed); byte-parity code-review of the serve() partition (single group at knob 1.0, `on_reconstruct` fires once at the old position/condition, token accounting equivalent) + the Set J parity tests; fixture census confirmed (no `correct` events — fork 11 by construction); the ruled single REAL ablation run's artifact re-read **number-for-number with every aggregate recomputed from the 24 raw rows**: verb ablation, real, arms gist-on 1.0 / gist-off 0.0, **24/24 paired + drift-checked, 0 band mismatches** (bands 2 and 3, int-equal per pair, distinct per-arm UUIDs), distance on p50 0.05/p95 0.16/max 0.174778 vs off 0.04/0.15/0.15939, **0 over-budget both arms, mean paired \|Δ\| 0.056025, gist-precision 0.833515 ON vs 0.703571 OFF**, fabrication 0.0 both (R7's deciding data — the ruling stays open, Jack's); no `longmem_eval_abl_*` residue and `longmem` untouched (agents/memories 0 rows, ledger 001–005); exit-code ordering code-confirmed (gate before corpus/provisioning, enforced by Set J's exploding monkeypatches). | 2026-08-12 |
| Deferred write processing v1 — Phase C1 (`deferred-writes.md`; **migration 006**, the first post-freeze migration since 005; the three dated 2026-08-12 C1 entries in `decisions.md` — the five spec rulings incl. the one-shot NULL→value completion sanction + byte-identity's third sanctioned cause, the `typology_confidence` salvage, and the build record): migration 006 (both write_cause CHECKs widened to admit `'enrichment'`; `memories` + `enrichment_pending`/`enrichment_attempts`/`enrichment_pending_triggers`; the `memory_enrichment_runs` per-attempt instrumentation table — a background worker has no response payload to ride — + the partial pending index) + four `SERVICE_DEFAULTS` knobs (kill-switch `deferred_writes_enabled` landing **0.0** — the sync path byte-for-byte pre-C1; flip is a Phase D question) + the ingest seam's extract-only refactor (`run_write_call` / `resolve_typology` / `escalate_with_retry` / `plan_spans` promoted module-level so the worker and the sync branch share ONE implementation) + the deferred branch (NLP pass, embedding, and atomic insert stay synchronous; write-call scalars land NULL under `enrichment_pending`; a declared typology stores at insert; the five non-importance triggers persisted — their NLP raw material is unrecoverable from the DB; honest zero LLM instrumentation) + `app\deferred.py` (skip-locked claims incrementing attempts at claim; ONE attempt per row per drain pass — the poll loop is the retry spacing; orphan sweep terminal-fills budget-spent pending rows without model calls; terminal fill byte-equivalent to the sync scoring-failed end-state; escalation double-failure stays soft; opportunistic embedding repair; started at BOTH construction sites, stopped before the pool closes) + completion by supersession (`'enrichment'` heads on both chains; **joins the drift-anchor set**; facts-only when a retelling won the head; cache eviction on EVERY completion shape) + `salvage_confidence` (the clamp's sibling seat, closing the carried item) + wire deltas (`IngestResult` nullable scalars + `enrichment_pending`; `/chain` + pending/attempts/run-log) mirrored in `Models.cs` + the Ledger `enrichment` badge + the load driver's `observe_total` p50/p95 series + suite **Set K** (`tests\test_deferred_writes.py`: 12 unmarked + 1 nlp) + the EIGHTH walker (`tests\verify_deferred_writes.py`). | floor-verifier **pass** (independent, 2026-08-13 — the build session was 2026-08-12 with an overnight pause before the verdict; re-ran everything itself): ruff check + `format --check` clean (47 files); full suite **108** ×2 + keyless subset **94**; all EIGHT walkers on fresh per-walker scratch — write **53** (the walker file confirmed untouched since A1 by git: the deferred-OFF byte-parity gate), deferred **51/51**, read **56**, cli-harness **51**, gate **51** (ledger pin mechanically +006, the 004/005 precedent), reconstruction **42**, authorial **34**, fact **34**; an independent 10/10 migration-shape probe (both CHECKs admit `'enrichment'` / reject junk, the run-log outcome CHECK, the partial pending index — all writes rolled back) + the same shape confirmed on live `longmem` via the postgres MCP; fresh 001→006 in order + idempotent re-run + product "6 applied, 0 pending"; product pristine re-checked AFTER all runs (ledger exactly 001–006, all eleven product tables 0 rows); both C# builds 0-warning; the 24-check console interop gate against the real served app (the API-lifespan worker site's live evidence); `apply_enrichment`'s eviction confirmed UNCONDITIONAL by code read and the scalar fill guarded `WHERE enrichment_pending` with COALESCE; the client-declared confidence 422 probed live (1.5 → ValidationError); `app\retrieval.py`'s diff exactly one comment (zero behavior) and the judge rule confirmed still reconstruction-heads-only; change-set surface == the intended 24 modified + 5 new files, nothing else moved; zero CR/control bytes across the changed docs and code. | 2026-08-13 |
| Reflection v1 — Phase C2 (`reflection.md`; **migration 007** `reflection_runs`; the three dated 2026-08-15 C2 entries in `decisions.md` — dossier, spec, build record incl. the four plan-batch rulings and the `[SETTLE-AT-BUILD]` resolutions): `app\reflection.py` (the ruled pipeline 1–9 — deterministic top-`reflection_sample_k` sampling by importance_norm × recency at the request's `client_timestamp` time basis with ties on `memory_id` and pinned rows on the PLAIN decay score; mechanical citation validation with the partial-drop count and the all-ungrounded 502; the non-LLM `compute_rrr` guard over the recent live window; the step-7 write transaction incl. component invalidation + per-affected-memory cache eviction with the re-render AFTER commit; threshold-gated soft consolidation absorbing by `invalid_at` with the server-computed source union; `ReflectionWorker` on the C1 lifecycle contract at BOTH construction sites with `sweep()` the deterministic no-timer entry, NO attempts ledger, and the per-agent `reflection_worker_enabled` kill-switch default **0.0**) + the judge-shaped role (`LONGMEM_MODEL_REFLECTION` loaded-never-required; standalone `build_reflection_provider` loud at first real use; Fake/Failing/Malformed/Real providers + prices) + db.py's reflection section (the 3-clause PURELY MECHANICAL trim query, the pressure aggregate computed-never-stored, run rows, the agent scan) + the constraint-follows-liveness join in `fetch_reconstruction_sources` + the extended model-free identity render (zero reflections ⇒ seed-verbatim — the parity contract) + the dialogue-seam move (the prose prompt rides the rendered document for the caller-frozen `identity_version`, resolved exactly like reconstruction's; the raw-seed asymmetry closed) + `POST /v1/agents/{agent_id}/reflect` (404/409/422/502) + the REPL `:reflect [consolidate\|no-consolidate]` + eleven `SERVICE_DEFAULTS` knobs + suite **Set L** (`tests\test_reflection.py`: 20 scenarios, ALL unmarked) + the NINTH walker (`tests\verify_reflection.py`) + the C# mirror field-for-field (`ReflectAsync` on client + session; interop beats 24 → 28) + the fourth-sanctioned-cause invariant amendments (CLAUDE.md + architecture §7) + the `mid-dialogue-gate.md` "only grows" expiry. | floor-verifier **pass** (independent, same date; re-ran everything itself): suite **128** full / **114**-subset (Set L 20/20 incl. the ASGI route contracts); all NINE walkers on fresh per-walker scratches — the new `verify_reflection.py` **60/60** (lettered A–F: migration 007 shape · the verb ladder · render/consolidation/dialogue-seam parity · trim + liveness + eviction · worker lifecycle at both sites · role shape), write **53/53 byte-untouched** and read **56/56 byte-untouched** (the zero-retrieval-change evidence), reconstruction **42**, cli-harness **51**, authorial **34**, fact **34**, gate **51** (ledger pin mechanically +007, the 004/005/006 precedent), deferred **51/51**; fresh 001→007 in order + idempotent re-run ("Up to date: 7 applied, 0 pending"); the 13-table census with `reflection_runs` sorted before `reflections` (confirmed against the real collation, never guessed); the judge-shape probe (real mode loads without the var; `build_reflection_provider` raises `ConfigError` naming it; fake mode = `FakeReflectionProvider`); both C# builds 0-warning (Debug + Release); the **28-check** console interop gate re-run LIVE by the verifier against a served fake-mode app on its own scratch — incl. the four reflect beats (grounded citations over the wire, pressure consumed, the 409 floor); product `longmem` pristine before AND after (ledger exactly 001–006, zero reflection rows — applying 007 to the product store is Jack's action, deliberately not the build's); believability = the real-mode stage-2 smoke vs the 2026-08-07 real baseline: checks **6/0 both**, gist_precision 0.7667 → 0.9, keyword_retention equal at 0.9375, remaining deltas single-run noise — **no regression** (`run_20260815T222459Z_pid_31388.json` vs `run_20260807T224423Z_pid_15844.json`); the verifier's one finding (three stale `SETUP.md` counts) fixed before commit; zero control bytes across the changed docs. | 2026-08-15 |
| Parameter compiler — Phase C3 (`parameter-compiler.md`; **migration 008** `compiled_bundles` + `compiler_runs`; the dated 2026-08-17 C3 build record in `decisions.md` — the seven plan-batch rulings): `app\compiler.py` (the compile service — stateless missing-pair work discovery over the K most recent live beliefs × the integrator vocabulary + the reserved `default`, deterministic newest-belief-first order so the budget cuts a stable prefix; one model call per pair with clamp-at-write to the frozen [0.25, 4.0] + the passthrough namespace filter with the dropped-keys count; the pure consume functions `resolve_scene_type` / `compose_bundle_weights` — per-axis products re-clamped at consume then clamped into [WEIGHT_MIN, WEIGHT_MAX], zero bundles = the identity; the standalone `CompilerWorker` — the THIRD background worker on the C1/C2 lifecycle contract at BOTH construction sites with `sweep()` the deterministic entry, NO endpoint/route/jobs-table by ruling, NO attempts ledger, per-agent `compiler_worker_enabled` kill-switch default **0.0**) + the judge-shaped role (`LONGMEM_MODEL_COMPILER` loaded-never-required — the third of its kind; standalone `build_compiler_provider` loud at the worker's first real compile; Fake/Failing/Malformed/Real + prices) + db.py's parameter-compiler section (append-only bundles, liveness DERIVED by join — belief invalidation/consolidation evicts with zero bundle writes; run rows) + the dialogue-seam consume splice (`scene_type` on the turn request; unknown → default + flag, log-and-continue; the six instrumentation terms incl. `bundle_fetch_ms`; `resolve_dialogue_weights` untouched) + the session/CLI surface (`:compile` sweep semantics with the visible count; `:scene <type>` extended to session state riding turns) + four `SERVICE_DEFAULTS` knobs + the `scene_types` config key + suite **Set M** (`tests\test_set_m_compiler.py`: 21 scenarios, ALL unmarked) + the TENTH walker (`tests\verify_compiler.py`) + the C# mirror field-for-field (`SceneType` on request + session; the six instrumentation properties; interop beats 28 → 32) + the stale-text corrections (architecture §10 typed core / §12 route + verb counts; README; docs\README). | floor-verifier **pass** (independent, same date; re-ran everything itself): suite **149** full / **135**-subset (Set M 21/21); the TEN walkers — the new `verify_compiler.py` **48/48** (lettered A–F: migration 008 shape · the compile ladder · window/liveness/eviction incl. the consolidation collapse · consume parity + the per-scene re-rank flip with hand exponent math · worker lifecycle at both sites · role shape), write **53/53 byte-untouched** and read **56/56 byte-untouched** (the zero-retrieval-change evidence), reconstruction **42**, cli-harness **51**, authorial **34**, fact **34**, gate **51** (ledger pin mechanically +008), deferred **51/51**, reflection **60/60** on a FRESH scratch — the verifier's one finding: `verify_reflection`'s B6/E9 DB-global counts make it non-re-runnable on the persistent `longmem_test` once its own prior green run has landed rows there; root-caused as the already-carried walker-scratch item's SECOND bite, pre-existing, not a C3 regression (the fresh-scratch run is that walker's documented precondition); fresh 001→008 + idempotent re-run ("Up to date: 8 applied, 0 pending") with all three CHECKs' teeth probed; the 15-table census ('compiled' < 'compiler' < 'corrections' confirmed on the real collation); the judge-shape probe (real mode loads without the var; `ConfigError` NAMES it; fake = `FakeCompilerProvider`); both C# builds 0-error; the **32-check** interop gate re-run LIVE (incl. the four scene-type beats: absent → default neutral echo, configured echoes itself, unknown flagged, bare boundary clears); product `longmem` pristine (ledger exactly 001–006, zero reflections rows, no compiler tables — applying 007 + 008 is Jack's action, deliberately not the build's); believability = the real-mode stage-2 smoke vs the 2026-08-07 baseline: checks **6/0 both**, keyword_retention equal at 0.9375, gist_precision 0.7667 → **0.85** (above baseline; C2's single-run 0.9 sits in the same noise band; the compiler was default OFF for the run), fabrication_rate mean 0.0833 → 0.1667 on the SAME single fabricated entity (a per-telling denominator effect, not a criterion) — **no regression** (`run_20260817T141601Z_pid_25616.json` vs `run_20260807T224423Z_pid_15844.json`); `resolve_dialogue_weights` byte-identical to HEAD (SHA-1 matched); no UPDATE/DELETE writer on the bundle tables anywhere in `app\`. | 2026-08-17 |
| Dissonance path + diegetic-correction event — Phase C4 (`dissonance.md`; **NO migration — the ruled per-target scope fact**: the `corrections` table + both diegetic `write_cause` values had waited in 001 since day one, so the ledger stays 001–008 and ZERO ledger-pin tests moved; the dated 2026-08-17 C4 build record in `decisions.md` — the eight plan-batch rulings incl. ruling 2 REUSE-THE-RECONSTRUCTION-ROLE against the recommendation): `app\dissonance.py` (the pure mechanical decision — `resistance = importance_norm × typology_mult(memory) × rigidity` vs `challenge = weight × typology_mult(challenge)`, strict > updates, ties defend, NULLs resolving through `importance_neutral` / `dissonance_rigidity_default` / the `resolve_typology` tail, every clamp mirroring its existing bound; `assemble_dissonance_prompt` forking the stance task block on the DECIDED verb with the reconstruction output contract verbatim; `DissonanceService.confront` — reads → decision → the single-item retell through `providers.reconstruction` BEFORE the transaction, `DissonanceCallError` → 502 nothing written) + `db.apply_diegetic_correction` (ONE chain-preserving transaction: supersede the live head at t_e, insert the verb-typed head, insert the `corrections` record — `source_event` via Jsonb — evict every cache row; CAS via the shared `_StaleHeadError`; NO pin check, NO drift check, NO fact statements — the exemptions are the absence of code) + `fetch_agent` widened with `rigidity` + `fetch_memory_dissonance_inputs` + **`POST /v1/events/diegetic-correction`** (the fourteenth route, the third diegetic event; 404 unknown/foreign, 409 CAS, 422, 502) + ruling 4 in `app\reconstruction.py` (`FIXED_CONSTRAINT_ANCHORS = {authorial_correction, update_with_resentment}` at the item builder + the ablation partition; the fork the 2026-07-17 authorial spec deferred, closed) + the chain inspector's `corrections` block (defaulted, additive) + the REPL `:confront` (verb + both sides + IDs; session effective time) + six `SERVICE_DEFAULTS` knobs (the typology-multiplier table serving BOTH sides; no `dissonance_enabled` — consciously declined, the event is client-invoked) + suite **Set N** (`tests\test_set_n_dissonance.py`: 23 scenarios, ALL unmarked — the Set A diegetic pair landed; `ctx.seed` grew the `typology` fixture param) + the ELEVENTH walker (`tests\verify_dissonance.py` — per-run fixture scoping instead of a kill-switch flip, no worker exists) + the C# mirror field-for-field (`DiegeticCorrectAsync`, `NpcSession.ConfrontAsync`, the chain-result corrections mirror; interop beats 32 → 36) + the ruling-8 architecture.md fixes (the `write_cause` enum line + the drift-anchor set line, both missing `enrichment` since C1). | floor-verifier **pass** (independent, same date; re-ran everything itself; its ONE finding — the product ledger at 001–008 against the dispatch's 001–006 expectation — adjudicated by Jack the same day as HIS OWN morning apply of 007+008, the reserved operator action performed ~90 min before the session's first tool call, ledger rows byte-exact and zero content-table writes; converted to a clean pass on his confirmation): suite **172** full / **158**-subset at the pins (Set N 23/23); all ELEVEN walkers green SERIALLY on a fresh `longmem_test` — the new `verify_dissonance.py` **38/38** (lettered A–E: the schema-in-waiting shape incl. corrections CHECK teeth + the fact chain REJECTING the diegetic verbs + the ledger still at 008 · the verb ladder with hand math incl. the exact tie + the per-agent verdict flip · chain/eviction/anchor incl. the coherent timeline + stacking · the read-path effect incl. the session runner's `:confront` core under time travel · pin/CAS/error ladder with rollback proof), `verify_reconstruction` **deliberately re-opened and re-closed at 46/46** (42 + the [13] ruling-4 section: the accepted head IS the anchor and the fixed constraint, a live post-event read reconstructs from it, rationalization never re-anchors) — the other NINE walkers byte-untouched at their criteria (write 53, read 56, cli-harness 51, authorial 34, fact 34, gate 51, deferred 51, reflection 60 on the sweep-fresh scratch, compiler 48; the zero-retrieval-change evidence); migrate no-op re-run "Up to date: 8 applied, 0 pending" on the scratch; both C# builds 0-error/0-warning (Debug + Release); the **36-check** interop gate re-run LIVE against a served fake-mode app (beat [14]: verb + IDs over the wire, both decision sides recomputed client-side from the echoed inputs, the chain read's correction record, the 409 CAS); product `longmem` content tables untouched (zero corrections rows; the only product change = Jack's two ledger rows); believability = the real-mode stage-2 smoke vs the 2026-08-07 baseline: checks **6/0 both**, gist_precision 0.7667 → **0.825**, detail_recall 0.7627 → 0.8213, keyword_retention equal at 0.9375, fabricated entities **1 in both** (the rate-mean move 0.0833 → 0.1667 is the same single entity concentrated on one memory — noise at n=6) — **no regression** (`run_20260817T185412Z_pid_18788.json` vs `run_20260807T224423Z_pid_15844.json`, verified by reading both artifacts); the complete `app\` diff carries exactly FOUR SQL write statements, all inside `apply_diegetic_correction`; a third documented bite of the carried walker-scratch item recorded (the elder correction walkers' DB-global corrections-emptiness asserts vs the [13] section — resolved by scoping [13] to chain rows + eviction only, the decisions entry). | 2026-08-17 |
| Agent-state read + async observes — Phase C5, client contract completion (NO spec doc by design — the approved plan was the spec, the contract lives in `unity-client.md` + `architecture.md`; **NO migration — the ruled per-target scope fact**: the gauge is computed-never-stored and every table/index was pre-laid in 001/007/008, both newest index comments having named this read; the dated 2026-08-17 C5 build record — seven plan-batch rulings, ALL recommended options taken): **`GET /v1/agents/{agent_id}/state`** (the fifteenth route, the **FOURTH unscored-by-contract member** — the ruling `reflection.md` pre-assigned to this task; 404 unknown agent, 422 `runs_limit` bounds; pass-through) over `RetrievalService.agent_state` in the unscored inspector section — the STANDARD payload: the stored row AS STORED (`config` verbatim, SERVICE_DEFAULTS never merged), identity currency SELECT-only (never the `ensure_` upsert), the pressure gauge via the existing `reflection_pressure_mass` (one implementation with reflect, the loud norm guard verbatim), live beliefs in the ruled compiler-window order (the first `compiler_window_k` rows ARE the compile window), the newest live bundle per (reflection, scene_type) with liveness DERIVED and `passthrough` verbatim (its recorded integrator read made real), both workers' run logs newest-first via new limited fetchers (the walker-facing oldest-first fetchers byte-untouched) + `fetch_agent` widened additively with `name` + four new SELECT-only fetchers + `AgentStateResult` + four full-column sub-models (the EnrichmentRunOut precedent) + suite **Set O** (`tests\test_set_o_agent_state.py`: 10 scenarios, ALL unmarked) + the TWELFTH walker (`tests\verify_agent_state.py` — per-run fixture scoping, runs LAST in sweeps; its section A makes the no-migration scope fact assertable) + the C# mirror (`AgentStateAsync` + five DTOs in the inspector-reads section, client-only by the inspector precedent; NpcJson unchanged — the tri-state rides nullable value types) + **`NpcSession` fire-and-forget observes** (`ObserveAndForget` stamping world time synchronously at the call — arrival order cannot reorder the record — + `PendingObserves` + `OnObserveFailed` + `DrainObservesAsync` throwing ONE AggregateException of the typed failures, cleared on drain; no queue, no pump, no retry, **no auto-drain by ruling** — "drain at scene edges" is integrator guidance, E2 places the drains; no ConfigureAwait(false), the standing contract; C1-orthogonal — zero backend changes for this half; Unity adapter/driver untouched by ruling) + interop beats [15]/[16] (the gate 36 → 50). | floor-verifier **pass** (independent, same date; re-ran everything itself): suite **182** full / **168**-subset (Set O 10/10, source-confirmed unmarked); all TWELVE walkers green SERIALLY on a fresh `longmem_fv_<pid>` scratch (created → migrate "8 applied" → idempotent no-op re-run → dropped), elder-first with the new walker LAST — write 53, read 56, cli-harness 51, authorial 34, reconstruction 46, fact 34, gate 51, deferred 51, reflection 60, compiler 48, dissonance 38, **agent_state 26** — the eleven elder walkers **byte-identical to HEAD** (the zero-scoring-change evidence; `app\retrieval.py`'s diff is exactly two additive hunks — one import, one appended method); both C# builds 0-warning full-rebuild Release (`--no-incremental`); the **50-check interop gate re-run LIVE** against a served fake-mode app on recreated `longmem_smoke` (beat [15]: the as-stored row echo, `config` verbatim, identity currency agreeing with the session's frozen version, beat-[12]'s beliefs riding the read, pressure > 0, EMPTY run logs + RunsLimit 100 — the endpoint/worker split visible over the wire, the 404; beat [16]: fires return with calls in flight, a dialogue turn completes UNDRAINED, the drain joins, both rows land in CALL-time world order, ONE AggregateException carrying the typed 404, the event fired once, a clean second drain); product `longmem` pristine **before and after** every run (ledger exactly 001–008, all 14 product tables 0 rows); **zero writes proven three ways** (diff token scan — zero SQL write tokens in the added app lines; the walker's F1 row-count byte-identity; F2 never-compiled-stays-never-compiled); NpcSession spot checks — the tracker catches its own exception (nothing reaches UnobservedTaskException), drain clears failures before throwing, the cancel lane throws before the clear (an abandoned drain is not a drain), `DrainObservesAsync` has zero call sites besides its definition (no auto-drain proven by absence), no ConfigureAwait(false) anywhere in Core; the norm-guard ValueError text verbatim-identical to `reflection.py`'s; docs propagation verified working-tree (FOUR unscored members across CLAUDE.md / architecture.md §6+§12 / test-suite.md / tests\CLAUDE.md / unity-client.md; the arrival-epoch honest caveat in unity-client.md; `status.md` at 12,048 B — the ruled tripwire held); believability = the C3 evidence class per the build record (zero retrieval/scoring/dialogue changes; Sets A–N + the elder walkers reproduce green), the full-compare flag standing. | 2026-08-17 |
| Purge endpoint — Phase C6, the release-blocker + the SOLE sanctioned content DELETE (contract in `architecture.md` §12, NO spec doc by design; **NO migration — the ruled per-target scope fact**: purge only deletes from the existing 001–008 tables, the ledger stays at 008 and ZERO ledger-pin tests moved; the dated 2026-08-18 C6 build record — four plan-batch rulings, ALL recommended options taken: per-memory scope / the DELETE verb / no guard / no migration): **`DELETE /v1/memories/{memory_id}`** (the sixteenth route; 404 unknown, 422 malformed; pass-through) over `IngestService.purge_memory` → `db.purge_memory` — ONE `conn.transaction()` opened by a `SELECT ... FOR UPDATE` existence-lock (the clean unknown-id → None/404 before any DELETE runs, plus race safety against a concurrent correction/enrichment), then the seven child-before-parent DELETEs forced by the bare NO-ACTION FKs (corrections → reconstruction_cache → memory_fact_versions → memory_enrichment_runs → memory_gist_spans → memory_details → memories, each scoped to the one memory_id), honest per-table rowcount counts + `PurgeOutcome`/`PurgeResult` (memory_id + the six per-table counts + total_ms) + the `db.py` header's sanctioned-write enumeration gaining purge as the FIRST content DELETE and reconciling its stale eviction list (two named → four real; `apply_reflection`/`apply_diegetic_correction` had joined with C2/C4) + reflections SURVIVE by design (the un-FK'd source_memory_ids may dangle — purge honesty; the `parameter-compiler.md` C6 note CLOSED: purge does not reach reflections) + suite **Set P** (`test_set_p_purge.py`: 7 scenarios, ALL unmarked; 182 → 189 full / 168 → 175 subset) + the THIRTEENTH walker (`verify_purge.py` — 21 assertions, sections A–G, id-scoped, incl. the wire contract over httpx.ASGITransport). | floor-verifier **pass** (independent, same date; re-ran everything itself): `verify_purge` **21/21** (A–G) on a FRESH pid-scoped `longmem_fv_c6` (create → migrate "8 applied, 0 pending" → run → dropped) — the seven-table delete + honest counts (B3–B8), zero-collateral (C1/D1), the survival boundary (E1–E4: the agent + the identity_component whose only referencing gist span was purged + the reflection whose source_memory_ids still names the purged id), unknown → None/404 (F1/F2), the wire contract 200/404/422 (G1–G4); suite **175**-subset (the "not nlp" gate) green **twice** (determinism; Set P 7/7 among them); migrate idempotent no-op "8 applied, 0 pending", no 009 added; product `longmem` pristine **before and after** (all 14 product tables 0 rows, ledger 001–008); the invariant re-checked — the ONLY new content DELETEs are the seven inside `db.purge_memory`, one transaction, touching nothing else (git diff: C6 added exactly those seven DELETEs + the FOR UPDATE lock, ZERO new in-place UPDATE of stored content; the four pre-existing DELETEs stay the reconstruction_cache evictions) + SQL-containment `test_repo_hygiene` green (no SQL in the route/service/schema) + ruff check and format --check clean (58 files); a **live uvicorn beat** (real `app.serve`, fake mode, scratch DB): DELETE → 200 + counts → chain 404 → reflection survives (agent-state read) → re-DELETE 404. Doc-honesty note carried to Jack: `write-path.md`'s two-item "Deferred, documented only" list was stale for BOTH entries (diegetic built C4, purge now C6) — reframed to "Built since" rather than flip purge alone. **Re-verified, not rewritten, 2026-09-02** with the per-agent extension (row 33's pass): `verify_purge` **36/36** (A–G intact, section H added), `purge_memory` now routes through the shared `_delete_memory_rows` — the same seven statements, the same order, one transaction; Set P 14/14. | 2026-08-18 |
| Concurrency cap — Phase C7 Stage A (audit R8; the `ModelCallGate`; NO spec doc by design — the approved plan was the spec; **NO migration — the request-path wait rides the pydantic `DialogueTurnInstrumentation`, the ledger stays 001–008 and ZERO ledger-pin tests moved**; the dated 2026-08-18 C7 fork rulings in `decisions.md` — **prompt caching DEFERRED on the Haiku-4.5 4096-token cacheable-prefix finding** against the ~0.5–1K-token dialogue/reconstruction heads, so C7 is the latency **PAIR** and leg 3 waits for the Phase-D model-slate lock): **`app\concurrency.py::ModelCallGate`** — one process-level gate holding an `asyncio.Semaphore(cap)` (the exact logical cap + the queue-wait measure) AND a bounded `ThreadPoolExecutor(max_workers=cap)` (so the default pool never binds first and keeps NLP + warm-up to itself), carried on the `Providers` bundle via a default-factory (the `dialogue`/`reconstruction` fake-default precedent — the ~15 direct `Providers(...)` test constructions stand unchanged, `build_providers` sizes it from settings), so every seam + worker reaches it as `self._providers.gate` with ZERO service-constructor churn; all **13 provider call sites** rerouted `asyncio.to_thread(provider…)` → `gate.run(provider…)` (ingest write/escalation/embed×2, retrieval query-embed, reconstruction reconstruct + drift-embed, dissonance retell, deferred embed, reflection reflect + consolidate, compiler compile), the streaming prose leg holding ONE slot for its whole life (`gate.acquire()` → `run_in_executor(gate.executor, _produce)` → release in `finally`, GeneratorExit-safe); the local spaCy/fastcoref NLP passes deliberately LEFT on the default pool (CPU-bound, already bounded, and the knob names *model* calls — the scope decision the floor-verifier confirmed, not flagged); the **`LONGMEM_MAX_CONCURRENT_MODEL_CALLS`** knob (a `Settings` int field on the `judge_max_tokens` precedent — env-validated ≥ 1 with a loud `ConfigError`, in `load_env`'s overridable set, default `8` aligned with the DB pool `max_size`; nothing hardcoded) + **`gate_wait_ms`** on `DialogueTurnInstrumentation` (the prose leg's queue wait, defaulted 0.0 — `perceived_first_word_ms` already folds it into the honest TTFT) + gate `shutdown()` at BOTH teardowns (`api.py` lifespan finally; `session.py` `SessionRunner.close` behind an `_owns_gate` flag mirroring `_owns_pool`, set post-construction) + the FOURTEENTH walker (`tests\verify_concurrency.py`, id-scoped with a per-run agent suffix, re-runnable on the persistent scratch). | floor-verifier **pass** (independent, same date; re-ran everything itself against all seven done-when criteria): **`verify_concurrency` 11/11** — A cap enforcement (cap=2, N+2 concurrent: `in_flight=2 peak=2`, the extra 2 block, all complete once freed, peak never exceeds cap), B a failed call releases its slot (exception propagates, the next acquire is free), C the `gate_wait_ms` source (uncontended acquire 0.0 ms; a 200 ms-held contention → 200.32 ms), D seam wiring (an uncontended turn surfaces `gate_wait_ms == 0.0`, still produces prose, byte-identical across runs); the `-m "not nlp"` gate **175 passed** (the floor held) / **189 full**; the wiring audit — every `gate.run`/`gate.executor` site wraps a provider method and every remaining `asyncio.to_thread` is correctly non-provider (NLP ×4, `warm_pipelines` ×2, the REPL `input()`, the offline eval-runner judge ×3), no `run_in_executor(None, …)` left; the knob exercised — default 8, `'3'` → 3, and `'0'`/`'-1'`/`'abc'`/`'2.5'` each a `ConfigError`, `ModelCallGate(0)` a `ValueError`; **no 009** (filesystem 001–008, `git status` clean of migrations, DB ledger `008_parameter_compiler.sql`); the invariant re-checked — `git diff app\` adds ZERO `DELETE`/`UPDATE…SET`/`DROP` (the change only reroutes how provider calls reach threads), non-destructive storage + decay-vs-invalidation + the IDs+scores read contract untouched, byte-identity preserved (walker D3); ruff `check` clean on `app` + the walker. | 2026-08-18 |
| Provider path + per-agent purge — the OpenAI-compatible backend seam and the embedding knobs, plus `DELETE /v1/agents/{id}/memories` (ruled 2026-09-01 at the consumer-context scan; the four forks ruled 2026-09-02 at the plan batch — explicit selector / ZERO-PAD widths (the one divergence from the recommendation) / thin purge extension / Ollama + hosted OpenAI beats; NO spec doc by design — the approved plan was the spec; **NO migration — the ruled per-target scope fact for BOTH halves**: no schema touched, the per-agent verb deletes only from the 001–008 tables, the ledger stays at 008, ZERO ledger-pin tests moved; the dated 2026-09-02 build record): `TWICETOLD_MODEL_BACKEND` (`anthropic` default \| `openai`) + `TWICETOLD_MODEL_BASE_URL` + optional `TWICETOLD_MODEL_API_KEY` for all nine LLM roles over ONE `ChatBackend` seam (`AnthropicChatBackend` = today's request bytes, PINNED by Set Q + walker E; `OpenAIChatBackend` = chat-completions with `max_tokens`, `response_format=json_object` on the six structured calls, `stream_options.include_usage`, usage-absent → 0/0 + warn-once, thinking dropped + warn-once), the seven real roles keeping their prompts / parses / pinned wrappers and one backend shared by the bundle; the embedding role's own `TWICETOLD_EMBEDDING_MODEL` (the NAME a knob, default the locked slate's) + base URL + key, `dimensions=1536` on both paths and the width fit (narrower zero-padded — cosine/L2/IP exact — + warn-once, exact passes, wider refused loudly); the loud `load_settings` matrix (enum + thinking cross-check in both modes, presence rules real-only, fake mode keyless), the six keys in the override allowlist, the hosted base URLs passed explicitly (the stray `*_BASE_URL` env redirect closed); the per-agent verb over the shared `_delete_memory_rows` (`= ANY(%s)`, the forced FK order, reached by both verbs), `FOR NO KEY UPDATE` on the agents row + `FOR UPDATE` on its memory rows, `AgentPurgeOutcome`/`AgentPurgeResult` with summed counts, 200-with-zeros for an empty agent, reflections / identity / bundles / run logs surviving, the eighteenth route; the small-model quality warning beside the knob (`.env.example`, `SETUP.md` §4b); + suite **Set Q** (`test_set_q_provider_path.py`: 26 scenarios over `tests\provider_transport.py`'s canned in-process HTTP — offline, keyless; 193 → 226 full / 178 → 211 subset) + **Set P +7** (14) + the SIXTEENTH walker (`verify_provider_path.py` — 40 assertions, sections A–H) + `verify_purge` section H (+15, 36); four live beats (fake; Anthropic with the real key; hosted OpenAI `gpt-4.1-mini` clean and `gpt-5-mini` reproducing the documented `max_tokens` limitation by the ladder; Ollama — installed this session — `llama3.2:3b`+`nomic-embed-text` on the padding branch and `llama3.1:8b`+`qwen3-embedding:4b` on the `dimensions` branch, the pre-ruled fallback NOT triggered). | floor-verifier **pass** (independent, same date; re-ran everything itself against all nine done-when criteria + the floor beneath): suite **226** full green **twice** (72.86 s / 72.97 s, warning summaries byte-identical, all 14 third-party) / **211**-subset (Set Q **26**/26 collected + green, Set P **14**/14); all SIXTEEN walkers green SERIALLY, elder-first, on a FRESH pid-scoped `twicetold_fv_provider_<pid>` (provision_scratch → migrate no-op "Up to date: 8 applied, 0 pending" before AND after the sweep → dropped) — write 53, read 56, cli-harness 51, gate 51, reconstruction 46, authorial 34, fact 34, deferred 51, reflection 60, compiler 48, dissonance 38, agent_state 26, **purge 36** (A–H; H1–H14 the per-agent verb: summed counts over two full-chain memories, seven tables empty for both ids, zero collateral on the co-resident agent, agent/identity/reflection/bundle/run logs surviving, zeros for a known empty agent, None for unknown, wire 200/200-zeros/404/422), concurrency 11, prewarm 14, **provider_path 40** (A–H: the config matrix, both-backend wiring offline, seven-role JSON round-trips, streaming + usage-absent + error contracts, the Anthropic request bytes pinned E1–E4, embedding pad/pass/refuse F1–F6, the served turn over mock-backed Real* classes G1–G3 with IDs + scores, the empty-output → scoring_failed ladder H1); **no 009** (filesystem 001–008, product ledger 001–008); the invariant re-checked — the ONLY content DELETEs in `app\` are the seven set-scoped statements inside `db._delete_memory_rows`, reached by `purge_memory` and `purge_agent_memories` (git diff: seven `= ANY(%s)` DELETEs + the `FOR NO KEY UPDATE`/`FOR UPDATE` locks added, the seven per-memory DELETEs removed, ZERO new in-place UPDATE; the four pre-existing DELETEs stay the reconstruction_cache evictions; zero SQL tokens in the non-db.py diffs); nothing hardcoded (the only literals: `EMBEDDING_MODEL_DEFAULT`, the two explicit hosted base URLs, `PLACEHOLDER_API_KEY`); fake mode keyless (openai selector + no URL/key loads; `build_providers` all Fake* under both selectors); 12 loud config rows each a `ConfigError` by direct probe, 6 quiet rows load; the Anthropic parity pins green (Set Q's two pin tests + walker E); the route count 18 with `DELETE /v1/agents/{agent_id}/memories` → UnknownAgentError 404 / UUID 422; real-mode boot parity — the current `.env` loads backend `anthropic` with both keys present and no base URL, `.env.example` loads fake; a **live `app.serve` beat in fake mode** (own scratch): observe ×2 → turn (IDs + scores) → SSE → state → per-agent DELETE 200 {memories_deleted 2} → `/memories` 0 → re-DELETE 200 zeros → unknown 404 → state still 200; the **Ollama beat** through the same runner (`llama3.2:3b` + `nomic-embed-text` at 127.0.0.1:11434, no spend): the startup line `real providers: model backend=openai host=127.0.0.1:11434 ...`, the warn-once `768-wide ... zero-padded to the locked 1536`, both observes landing with real usage reported and no ladder degrade, the turn scored, the stream 83 chunks + result, the purge 200 {2}; product `twicetold` pristine **before and after** (all 14 content tables 0 rows, ledger 001–008 byte-identical); docs propagation verified working-tree (226/211, sixteen walkers, eighteen routes / sixteen in OpenAPI, both verbs in CLAUDE.md's invariant, the backend + padding paragraphs in architecture §3, `status.md` at 12,303 B — on the tripwire line); ruff check + format --check clean (67 files). Two stale comment lines it flagged (`providers.py` module docstring, `ingest.py` purge section) were corrected after the pass — comment-only. **Re-verified, not rewritten, 2026-09-11** with the token-limit-field knob (`TWICETOLD_MODEL_TOKEN_LIMIT_FIELD` = `max_tokens` default \| `max_completion_tokens`; ruled 2026-09-03, its forks 2026-09-11): the wire field is knob-selected on BOTH openai call paths (the default byte-identical, the flip byte-asserted with `max_tokens` absent, `max_completion_tokens` refused at load under anthropic), Set Q **28/28** (+2), `verify_provider_path` **41/41** (A9 + the seven-key A8) on a fresh scratch, suite 228 full / 213 subset, the Anthropic pins intact (E1–E4); live beat on hosted `gpt-5-mini`: every call **200** under the flipped field — the 2026-09-02 400s gone — the write scored real (scoring_failed=false, importance_raw 0.55, typology observed), escalation degraded by the documented ladder (reasoning-class content quality, not the wire; the row stands in `twicetold_smoke`, verifier-inspected). Independent floor-verifier pass same day, all eight criteria. | 2026-09-02 |
| Scene-boundary reconstruction pre-warm (probe-driven) — Phase C7 Stage B (NO spec doc — the approved plan was the spec; **NO migration — the ruled per-target scope fact**: an optional request field + reuse of `memories`/the vector index/`reconstruction_cache`, the ledger stays 001–008 and ZERO ledger-pin tests moved; the dated 2026-08-18 C7 fork rulings in `decisions.md` — pre-warm **probe-driven** over the probe-free recommendation, guardrail **reuses `serve`'s drift-budget refusal** over a new gist-precision gate): the scene-boundary handler's SECOND server-side consumer, folding today's off-camera warm-init trick into the boundary call. **`SceneBoundaryEvent.prewarm_context`** (optional; present + non-empty => warm, absent/empty => the OFF state — not sending it is off, the C4/C5 client-invoked-verb precedent) + **`IngestService._prewarm_reconstruction`** reusing **`RetrievalService.retrieve_dialogue_init`** (embed → fetch → score → `serve`) with the probe as the query, at the FRESHLY recompiled `identity_version` + the boundary's `client_timestamp` as `scene_started_at`/`as_of`, so `serve`'s reconstruction write-backs land the cache under the exact composed keys `(identity_version, band)` the first on-camera read looks up; the R8 gist-precision guardrail satisfied by INHERITANCE (reusing `serve` verbatim inherits its drift-budget refusal — candidate-vs-anchor cosine > `drift_budget_threshold` → refuse the write-back, cache the prior head — NO new gate); best-effort by a broad `try/except` returning a degraded record, so a warm failure NEVER fails the boundary (`RetrievalInstrumentation` mapped field-for-field into the new **`ScenePrewarmInstrumentation`** on **`SceneResult.prewarm`** — None unless a probe was sent) + `RetrievalService` injected into `IngestService` (optional param default None, so the many direct `IngestService(...)` test constructions stand; both construction sites build retrieval BEFORE ingest and pass it — `api.py` `_lifespan`, `session.py` `SessionRunner.create`; a `TYPE_CHECKING` import breaks the ingest↔retrieval cycle since `retrieval.py` imports `ingest.py`) + the FIFTEENTH walker (`tests\verify_prewarm.py`, per-run agent suffix, id-scoped, re-runnable on the persistent scratch). | floor-verifier **pass** (independent, same date; re-ran everything itself against all five done-when criteria + the floor beneath): **`verify_prewarm` 14/14** — A a probe warms the cache (`cache_misses=2`, `reconstruction_cache` 0 → 2 cold-to-warm, identity recompiled alongside, not degraded), B the next same-basis init is CALL-FREE (`cache_misses == 0`, `cache_hits=2`, `reconstruction_input_tokens == 0` / `_output_tokens == 0` — the zero-token signal, since `reconstruction_ms` still times the theta partition + cache fetch on a hit), C no-probe = the unchanged off state (`prewarm is None`, identity still recompiled, cache stays cold), D fail-quiet (boundary `accepted` with `prewarm.degraded=True`, no exception escaped); the drift-budget refusal confirmed INHERITED structurally (the warm routes `_prewarm_reconstruction` → `retrieve_dialogue_init` → `serve`, whose refusal carries the threshold/cosine/refuse/cache-prior logic; the warm path adds none — already floor-verified by `verify_reconstruction`); `-m "not nlp"` **175 passed** (the pre-C7-B scene-boundary tests green — the off state unchanged) / 189 full; the C7-A floor re-checked directly (`verify_concurrency` **11/11**, `ModelCallGate` untouched); **no 009** (filesystem 001–008, `git status` clean of migrations, DB ledger 008); the invariant re-checked — the warm's only writes are through `serve`'s existing sanctioned reconstruction write-back + cache writes (grep over added app lines: zero new `DELETE`/`UPDATE`/`INSERT`/`.execute`), nothing hardcoded (drift threshold / theta / cap stay knobs); ruff clean on `app` + the walker. | 2026-08-18 |

---

## Re-verification passes

Not floors — floors are layers, and the table above is the count. This records independent
floor-verifier passes that re-ran **existing** floors rather than landing a new one, because the
evidence is worth keeping and the count must not drift upward for work that added no layer.

**2026-07-28 — full-repo audit remediation, all eighteen floors re-verified. VERDICT: pass.**
The subject was not a new layer but a remediation pass touching every floor, so the floor beneath
it was the whole table as of `49a635b`. Two rounds: the first returned **fail** on one real point
(a `ruff format` run with `target-version` briefly set had stripped the parentheses from
`except (A, B):` in `app\ingest.py` — the exact hazard `ruff.toml`'s own comment describes,
committed alongside that comment; found by a token-stream diff after an AST diff came back clean,
since the two forms are AST-identical). Fixed, guarded by `tests\test_repo_hygiene.py`, and
re-run.

Second round re-ran, independently: seven walkers on fresh scratch (53/56/67/51/42/34/34 — write
path grown by two for the malformed-output ladder row); full suite **55** twice for determinism +
keyless subset **48** with both API keys provably scrubbed; `ruff check` clean and
`ruff format --check` 35/35 with `target-version` confirmed unset; `migrate.py` no-arg "5 applied,
0 pending" and schema frozen; `longmem` pristine (ten product tables 0 rows, ledger exactly
001–005, no scratch residue); both C# projects 0 warnings and the console interop gate **23/23**
live against a served backend *(corrected 2026-07-29 — this note originally said 21/21, a stale
figure: the [11] client-timing checks had already grown the gate to 23 mid-audit, and a fresh
2026-07-29 live run measured 23/23)*; every tracked text blob LF with **no lone CR**; the committed Unity
DLL diffed against a fresh build down to five build-identity fields with **0 differing bytes
outside them**; the guard mutation-tested to prove it can fail; and the `status.md` three-way
split proven **lossless by extraction** against `49a635b` — 18/18 floor rows and 851/851
non-empty session-log lines byte-identical. *(2026-07-29 re-proof: all 18 rows and all 851 lines
remain present; six of them — five session-log lines and the Target-A floor row — later gained
dated in-place annotations from phase 7's research-folder move, so "byte-identical" was true at
the split-time measurement, not at wrap-up.)*

Invariants re-checked and intact: no new in-place UPDATE of stored content, no new DELETE, read
payloads still carrying IDs and scores, nothing integrator-configurable newly hardcoded.

**Reported blocked, then RESOLVED 2026-07-29 — now a floor (the stage-2 row above).** In both the
2026-07-28 audit and the 2026-07-29 re-audit the gate's MCP tools were unreachable — root cause:
session-start ordering (the session began before the Editor opened, so no `mcp__UnityMCP__*` tools
registered). With Unity opened first on 2026-07-29 the bridge reached both the main loop and the
floor-verifier subagent; the gate ran 8/8 in fake mode, the floor-verifier re-ran it 8/8 live, and
a real-mode pass corroborated it (+1061 frames on check #8). It mattered more than usual because
the plugin DLL was rebuilt three times across 2026-07-27/28 — provenance re-proven clean.

**2026-08-07 — full-repo audit (run parallel to Jack's offline gold labeling): the standing
floors mechanically re-verified on today's tree. VERDICT: pass.** No layer landed; the
twenty-three-row table is unchanged. Re-run this session, independently of every recorded
result: `ruff check` clean + `format --check` 43/43 at the 0.15.21 pin; full suite **86
passed** (108 s); all seven walkers green, each on a fresh per-walker scratch — write **53**,
read **56**, CLI-harness **51**, gate **51**, reconstruction **42**, authorial **34**, fact
**34** — exactly the stage-3 row's counts; `db\migrate.py` no-arg → "Up to date: 5 migration(s)
applied, 0 pending"; `longmem` pristine via the postgres MCP (ten product tables 0 rows, ledger
exactly 001–005, zero scratch residue after all runs); both C# builds 0 warnings; committed
Unity plugin DLL vs a fresh Release build — same size, **150 differing bytes, ALL
build-identity** (COFF stamp, MVID, PDB GUID/checksum, embedded revision sha — the committed
DLL embeds ancestor `2b15b15`, and zero commits have touched `client\NpcMemory.Core\` since it
landed, so IL/metadata are byte-identical; this also reconciles the 2026-07-29 "150 bytes" and
2026-08-04 "sha-identical" provenance claims — both true at their build shas); the console
interop gate **24/24** fake-mode against a served backend on scratch `longmem_smoke` (a first
attempt failed on an audit-runner stdout-pipe deadlock — diagnosed as runner-side, not repo:
port free after, product DB pristine, clean 24/24 on the file-backed re-run); eval-harness fake
e2e — `run` **6/6** exit 0, `drift-validate --plumbing` exit 0 with 0 over budget and the
self-check matching, judged `--plumbing` exit 0 labeled plumbing-only with **0 judge_failed**
across 24 sf + 24 abstention + 30 faithfulness facts; every blob at HEAD carries zero CR bytes
(lone-CR included); gold-file blind intact (78 rows, 24/24/30, every label null, no
verdict-shaped keys). Honest note: one eval e2e invocation first ran REAL by omission of the
fake override (the `.env` real-mode trap) — ~$0.03 measured on the `run` leg plus a real
`drift-validate` (which incidentally corroborated the ruled 2026-08-05 numbers: p50 0.06 / max
0.117 vs 0.030 / 0.120, both far under the 0.35 budget) and a judged leg killed within ~a
minute; scratch dropped, all three re-run fake. The audit's six-file doc-drift fix set is
recorded in the session log; the Unity Play-mode gate was deliberately out of scope (closed
2026-08-05, fake-only by ruling; Editor not open before session start).

**2026-08-26 — F0 audit/test/improve re-verification (independent floor-verifier pass, same
day; the dated F0 entry in `decisions.md` is the rulings + build record).** The F0 change set
(the turn-path overlap; the `LONGMEM_DB_POOL_MAX_SIZE` knob; the driver's `pre_prose` series;
the dialogue-prompt UUID strip; the `_WRITE_SYSTEM` voice clause; the init 404/422
route-contract test; the register/doc repair incl. THIS FILE's rows 28–32 moved back inside
the table block — a byte-pure 5-line relocation, verifier-checked by sorted-multiset diff)
re-opened the cli-harness, read-path, write-path, and gate floors and re-closed them. The
verifier's own runs: suite **193 full / 178 subset** green; all FIFTEEN walkers green
SERIALLY on a fresh pid-scoped scratch, elder first — write 53, read 56, cli-harness 51,
authorial 34, reconstruction 46, fact 34, gate 51, deferred 51 (banner format differs from
the runner's scan — solo re-confirmed **51/51** on a second fresh scratch), reflection 60,
compiler 48, dissonance 38, agent_state 26, purge 21, concurrency 11, prewarm 14; the
17-beat C# interop gate **53 checks GREEN** against a served fake-mode app on recreated
`longmem_smoke` (builder's run, same day); knob validation ad hoc ('3' → 3; '0'/'-1'/'abc'
each a loud `ConfigError` naming the var; unset → 8; conftest + all fifteen walkers
byte-untouched at bare `build_pool(uri)`); believability READ not re-run (the C4 precedent):
real-mode `run_20260826T223224Z_pid_5008.json` vs the D1 baseline
`run_20260819T055215Z_pid_22240.json` — gist_precision 0.8232 → **0.8309**, detail_recall
0.6603 → **0.8448**, fabrication_rate 0.0435 → **0.0159**, keyword_retention equal at
0.8478, fabricated entities **1 in both** — NO regression, both prompt changes landed on it
(fake dry-run artifact present, provider_mode fake); invariants re-checked — zero
DELETE/UPDATE tokens in the added `app\` lines (the knob REMOVES a `max_size=8` hardcode),
the IDs+scores read contract untouched, **NO migration** (filesystem and product ledger both
exactly 001–008; product `longmem` content tables 0 rows before and after); latency evidence
read from the builder's five driver artifacts — baseline perceived p50 826/835 ms, overlap
arms 873/887 with the +40–50 ms attributed to provider-side first-word drift (embed + SQL
controls flat; walker byte-identity excludes the code change), the overlap's target
component **non-embed pre-prose 27.3/27.7 → 20.5/18.4 ms**, the UUID strip's **dialogue
input 40.9–42.4k → 20.6k tokens/100 turns** (all-in **$0.109–0.112 → $0.0841**/100 turns),
final run p50 917 / p95 1227. Not a new layer — **the count stays 32**.

**2026-09-02 — the `longmem-npc` → `twicetold-npc` rename, independent re-verification.
VERDICT: pass (code + infra); Unity criterion BLOCKED, carried.** No layer landed — a
whole-repo rename (env prefix `LONGMEM_`→`TWICETOLD_`, DBs/container/volume, the C# namespace
`NpcMemory` deliberately unchanged), so the count stays 32. The floor-verifier re-ran in a
fresh context, independently of the build session: the **grep gate** — `git grep -in longmem`
returns 398 hits across 76 files, every one inside the ruled allowlist (immutable migration
001 comment; the three append-only register bodies with twicetold-npc H1s; all `docs\research\*`
+ `external-audit-*` + retired `split-brain-streaming.md`; spec docs' dated/BUILT/SETTLE-AT-BUILD
sections; third-party `LongMemEval`/`LongMemory`/`langmem`/`longmem.dev`; frozen
`data\eval\gold\*.json`; README's continuity note; and the deliberately-kept local folder path
`C:\Users\jacks\Projects\longmem-npc`), with **zero** `longmem`/`LONGMEM_` in any code/config
file (`app\`, `tests\`, `.env.example`, `data\eval\arms`, `db\migrate.py`, migrations 002–008,
the C# and Unity sources) and the living-contract docs (`architecture.md`, `CLAUDE.md`,
`SETUP.md`) clean; **full suite 193 passed**; **four walkers** across eras on fresh
`twicetold_test` (write 53, agent-state 26, purge 21, reflection 60 on its own scratch); the
**`PRODUCT_DB`↔`.env` lockstep** — `PRODUCT_DB == dbname == "twicetold"` and
`provision_scratch(base,"twicetold")` raises (the guard protects the real product DB), with a
real-mode `load_settings()` boot on the swapped `.env`; **migrate idempotency** — no-arg
`migrate.py` → "Up to date: 8 applied, 0 pending"; and the **committed Unity DLL** (rebuilt +
copied this session) carries `"twicetold-npc API error"` (utf-16-le) and NOT the old string.
Invariants re-checked intact (no new in-place UPDATE/DELETE, read payloads still carry IDs +
scores, nothing configurable newly hardcoded — `PRODUCT_DB` is a guarded product-DB name, the
connection stays `.env`-configurable). **Unity Editor compile/import: BLOCKED** — UnityMCP was
unreachable all session (the 2026-07-29 session-ordering root cause), so the Editor could not
run; the DLL is a drop-in binary whose only source delta is one string literal and the two
adapter `.cs` files changed one display string each, so breakage risk is near-zero but genuinely
unverified. Carried to F3 (which already owns the Unity MCP pin + committed-DLL staleness check),
to run once the bridge is up.

