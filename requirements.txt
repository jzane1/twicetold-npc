# twicetold-npc backend runtime dependencies (global Python 3.14, on PATH).
# Install (PowerShell):  python -m pip install -r requirements.txt
#
# The Postgres MCP server runs from its own isolated uv-managed Python 3.13 tool
# venv and is not listed here; it is a development-machine convenience only.
#
# NOTE (2026-07-13): spaCy model downloads must go through pip URLs below, NOT
# `python -m spacy download ...` — spaCy's downloader shells out to uv on this
# machine (uv is installed for the Postgres MCP) and fails outside a venv.

# storage
psycopg[binary,pool]==3.3.4
pgvector==0.5.0

# tooling: ruff is the mechanically-enforced formatter AND linter (CLAUDE.md;
# .claude\hooks\format-on-edit.ps1 runs `format` then `check` on every edit).
# PINNED 2026-07-28: it was unpinned, and ruff's formatter output moves between
# releases — tests\test_set_b_decay.py silently fell out of format because of
# exactly that. The one tool the repo enforces mechanically must not drift.
ruff==0.15.21

# structural test suite (tests\test_*.py; walkers need only httpx) — ruled 2026-07-20
pytest==9.1.1
# httpx: ASGITransport vehicle for four walkers + the suite (pin closes the
# 2026-07-16 audit observation)
httpx==0.28.1

# API surface
fastapi==0.139.0
uvicorn==0.51.0

# model providers
anthropic==0.116.0
openai==2.45.0

# write-time NLP stack
spacy==3.8.13
fastcoref==2.1.6
vaderSentiment==3.3.2
# fastcoref 2.1.6 is incompatible with transformers 5.x at runtime
# (FCorefModel breaks on the renamed all_tied_weights_keys internals) — pin <5.
transformers<5

# spaCy models: lg = the write pass pipeline; sm = fastcoref's internal tokenizer.
https://github.com/explosion/spacy-models/releases/download/en_core_web_lg-3.8.0/en_core_web_lg-3.8.0-py3-none-any.whl
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl
