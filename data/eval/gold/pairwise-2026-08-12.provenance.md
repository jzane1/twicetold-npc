# Provenance — pairwise-2026-08-12.jsonl labels

**Source:** `emit-gold` over the first real compare artifact
(`compare-2026-08-12-artifact.json`, haiku vs sonnet-5, 2026-08-12), filtered to
`prose_pairwise` only — the sf/abst/rf rows a compare artifact emits carry colliding
item_ids across arms (arm B overwrites arm A in the agreement join index), so only the
`cmp:` pairwise rows are labelable with integrity. Fork-12 cap 30 (of 57 valid pairs;
5 judge_failed pairs skipped at emission).

**Labeled:** 2026-08-12, by a single blind reference pass of **Claude Fable 5**
(`claude-fable-5`): the gold-label workaround, a single model pass, since hand-labeling
was unavailable.
Blindness: the labeler saw only question/reply_a/reply_b (no arm identities, no judge
verdicts, no dimension scores) in an isolated staged copy whose **positions were
shuffled** — alternate rows had reply_a/reply_b swapped, and labels were un-swapped
mechanically at merge — so a fixed-position bias cannot leak into the labels. The
labeler was instructed on the pp-v1 dimensions verbatim and told to use ties sparingly.

**Result: kappa 0.3697 (raw 0.8333, n 30) — FAILS the 0.6 bar. Pairwise judged numbers
are NOT quotable.** Reference marginals 26 b / 4 a / 0 tie; judge marginals on the same
rows 25 b / 3 a / 2 tie. The five disagreements decompose as: 2 tie-granularity
mismatches (judge tie via its position-swap disagreement rule vs a decisive reference
label) and 3 genuine direction flips on close pairs. Both instruments agree
DIRECTIONALLY (sonnet-5 prose decisively preferred); the bar still rules — skewed
marginals make kappa fragile at n 30 with a tie-protocol mismatch.

**Jack's standing override:** any row Jack re-labels wins; `agreement` re-runs offline
in seconds against the compare artifact, kept in the local eval record, not in the
public tip.
